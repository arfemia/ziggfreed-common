apply from: "$rootDir/gradle/zc-module.gradle"

// World identity (selectors, name index, match ranking), atmosphere and placement helpers, and the
// world-map POI/discovery layer.

dependencies {
    implementation project(':zc-core')
    implementation project(':zc-entity')
}

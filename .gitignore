.gradle/
build/
.idea/
*.iml
out/
# The dev-server working dir at the repo root only: an unanchored run/ also swallowed the
# zc-encounter source package com.ziggfreed.common.encounter.run and its test twin.
/run/
local.properties

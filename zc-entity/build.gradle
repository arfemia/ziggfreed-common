apply from: "$rootDir/gradle/zc-module.gradle"

// Puppets, performers and the item-carried stat bridge. The world-eviction seam a performer's
// per-world bookkeeping registers against is a zc-core primitive, so this module rests on core
// alone.

dependencies {
    implementation project(':zc-core')
}

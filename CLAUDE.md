# CLAUDE.md - Ziggfreed Common

A **standalone Hytale utility mod** that holds shared, mod-agnostic primitives lifted (config-free) from the MMO Skill Tree mod and the Kweebec Nightmare minigame, so standalone Ziggfreed minigames AND the MMO Skill Tree consume ONE battle-tested implementation instead of re-deriving 3D sound / camera / HUD plumbing per mod (hyMMO consumes the SHARED PROGRESSION + COMMERCE ENGINES (`zc-progression` / `zc-objectives` / `zc-commerce`: its quests, achievements, shops, boards and currencies are this library's engines, as are its statistics counters, the placed-block ledger and the tracked-quest HUD), plus the dialogue engine, toast engine, JsonTreeUtil, AbstractRawJsonAsset, the `ui/UiRetint` retint engine + the `ui/theme` Palette/ThemeRecord model, and the `AbstractKeyedAssetConfig` fold, as a HARD runtime dependency; Kweebec Nightmare 1.0.0 consumes the full primitive set). **Status: v2.1.0 (unreleased and open; `gradle.properties` reads 2.1.0 because this cycle adds the `zc-encounter` boss framework beside a breaking `PlacedBlockLedger.Policy` change, and a consumer pinned at 2.0.0 must fail by name. 2.0.0 released 2026-08-31, the whole cycle developed under the internal 1.4.0 number, and the previous public release was 1.1.1)** (this cycle adds the reusable cast / ability RUNTIME layer [`cast/` + `cast.step/`](zc-cast/src/main/java/com/ziggfreed/common/cast/CLAUDE.md) - a GENERIC step-dispatch kernel (parameterized over a consumer's own context / step / result types, no baked-in step vocabulary), a per-consumer on-hit builder registry + a per-hit resolver (`HitContext`/`HitAction`/`HitResolver`) + armed-state store + a guarded observer registry, ray/block targeting, and the per-world tick partition (world-keyed queues, evictor fan-out, an abstract drain system riding the engine's own once-per-world-per-tick dispatch) plus a guarded particle seam - lifted config-free for a consumer that fires abilities, ships EMPTY of content, additive-only once frozen; the MMO is the first consumer; 1.4.0 also adds the puppet-presentation entity primitives - `entity/PlayerPuppetService` (clone a live `PlayerSkin` onto a networked spawned entity, held-item mirroring, a render-guaranteed animation surface, plus the paired `Scale` self-hide/reveal pair) and `entity/ItemPropEntityService` (a networked item-prop display entity) - lifted config-free out of a consumer's in-game-proven puppet spike harness and its own placed-input display, for the "mount + hide the player, show a puppet performing the work instead" presentation any consumer can build; 1.3.0 adds a settings-UI toolkit AND a generic settings-FORM engine built on it - `util/JsonOverrideWriter` (partial owner-file write-back over `JsonTreeUtil.deepMergeInto` + an array-by-key upsert, plus array-leaf write-back: a `List`/`String[]` value emits a JSON array, replacing wholesale), `ui/hud/HudPosition` (the lifted mod-agnostic HUD layout value), `ui/SettingsUiUtil` (a domain-free form-binding helper), the `Pages/ZigListRow.ui` CRUD row, and `ui/form/` (an ordered `FieldSpec` list rendered/refreshed/collected by `SettingsForm` into appendable rows, ready for a `JsonOverrideWriter` write-back, plus its five `Pages/ZigForm*Row.ui` templates, each with an optional per-row wrapping hint line) - the reusable pieces an in-game config page needs - PLUS two per-world-config primitives: `world/WorldNameMatcher` (the generic exact > longest-core prefix/suffix/contains > bare `*` per-world matcher, feature-matched to the MMO's `WorldRulesMatcher` so a `*Foo*` contains Match catches an `instance-`-prefixed, randomly-suffixed instance world) and `codec/JsonParentResolver` (generic raw-JSON `Parent`-chain resolution across store + owner-dir layers), backing a consumer's "one file per world, inherit from a base" schema (the mob-scaling mod's `Worlds/*.json` is the first consumer); 1.2.0 added the domain-free `scaling/` difficulty-scaling engine + a ref-less `HealthUtil.scaleMaxHealth(Holder,...)` overload + `EntityIdentifierUtil` `roleName`/`roleIndex` NPC role reads, the Phase-0 lift the MMO mob-scaling mod builds on; 1.1.0 added the `ui/UiRetint` retint engine + `ui/theme` value model + `world/AtmosphereService`/`ForcedMusicService`; see CHANGELOG) - the static primitives PLUS a generic branching NPC **dialogue engine** ([`dialogue/`](zc-dialogue/src/main/java/com/ziggfreed/common/dialogue/CLAUDE.md)) PLUS the **matchmaking lobby** ([`lobby/`](zc-instance/src/main/java/com/ziggfreed/common/lobby/CLAUDE.md)) and the **instance-experience layer** (a full party system, queue/end-game/leaderboard SCREENS, a codec-asset-driven instance preset + reward model with a full-inventory guard - see below); it SHIPS its `.ui` asset pack (`IncludesAssetPack:true`) and, as of the asset-driven-framework pass, the plugin now REGISTERS the framework asset stores (`setup()` calls [`asset/FrameworkAssetRegistrar`](src/main/java/com/ziggfreed/common/asset/FrameworkAssetRegistrar.java)) at `Server/ZiggfreedCommon/<Type>/` - see the asset-driven paradigm below.

> **The instance-experience layer (the reusable end-game + party/queue framework).** A consumer minigame (Kweebec now, future dungeons/raids) gets, mod-agnostically: a searchable-invite **party** ([`party/`](zc-instance/src/main/java/com/ziggfreed/common/party) - `Party`/`PartyService` + `PartyInvitePage`, the Queue button carrying a chosen preset), a unified **Play / queue-mode** screen ([`instance/play/`](zc-instance/src/main/java/com/ziggfreed/common/instance/play/CLAUDE.md) - `PlayModePage`: an asset-driven Public/Party/Solo chooser that morphs to a still-closable live roster whose launch timer ticks down on screen; atomic party group-join + immediate solo-launch in `lobby/`), an **end-game results** screen ([`instance/result/`](zc-instance/src/main/java/com/ziggfreed/common/instance/result) - `MatchResult` team breakdown + reward chips + leaderboard CTA via `ResultsPage`), a generalized bucketed **leaderboard** ([`instance/leaderboard/`](zc-instance/src/main/java/com/ziggfreed/common/instance/leaderboard) - `Leaderboard` + `LeaderboardPage`, lifted from Kweebec), an asset-driven **reward** model with the no-claim-with-full-inventory guard PLUS the DEFERRED-payout layer ([`instance/reward/`](zc-loot/src/main/java/com/ziggfreed/common/instance/reward/CLAUDE.md) - `InstanceReward`/`InstanceRewardGranter`/`PendingRewardStore`, `InventoryUtil.canFit`; `DeferredRewards` turns what a `Lootable` DECIDED into rewards a player is shown now and handed later, a preset naming its table by `RewardTableId` so a higher run score unlocks premium entries + more picks), a codec-asset-driven **preset** ([`instance/preset/`](zc-instance/src/main/java/com/ziggfreed/common/instance/preset) - `InstancePresetAsset` Pattern A + `InstancePresetConfig`), and the **generic Pattern-A registrar lift** ([`asset/`](src/main/java/com/ziggfreed/common/asset/) - `AssetStoreRegistrar`/`AssetMergeAdapter`). Each page takes an immutable `*PageDeps` + a locale-free `*Messages`/`*ScreenMessages` provider (the consumer supplies pre-built client-resolved `Message`s); pages reuse the shared `ZigFrames.ui`/`ZigButtons.ui` styles AND the lifted `ui/toast/` engine for in-page reward/feedback toasts (results reward strip, party invite/privacy actions, queue-joined). Reward chips are the shared `loot.reward.RewardChip` every payout surface in the library paints, built here by `RewardChipRenderer`: an item icon by id + an auto-generated "x{n} {ItemName}" label from the item's own engine display name (no per-reward `displayKey` needed). The party screen carries an owner public/private queue toggle. Kweebec's consumer wiring is its `experience/` package - the model for any future consumer.

> **The reusable ENCOUNTER / difficulty framework (Kweebec today; the MMO's future dungeons/raids are the target).** Beyond the end-game layer, a consumer gets mod-agnostic primitives to BUILD an encounter: per-entity timed effects + escalation bands ([`instance/effect/`](zc-effects/src/main/java/com/ziggfreed/common/instance/effect/CLAUDE.md) - `EntityEffectService.applyTimed`/`applyBand` + `EffectBand`/`EffectBandLadder`, the on-hit slow / debuff / pace-band seam over `EffectControllerComponent`, lifting Kweebec's hunter-pace swap); weighted spawn rosters + a wave director ([`instance/encounter/`](zc-instance/src/main/java/com/ziggfreed/common/instance/encounter/CLAUDE.md) - `SpawnRoster<T>`/`SpawnUnit<T>` weighted + tier-gated picks, `EncounterDirector` cooldown/cap/per-round-max gating; PURE logic, no engine deps); floor-snapped runtime placement ([`world/SpawnPlacement`](zc-world/src/main/java/com/ziggfreed/common/world/CLAUDE.md) - ring/near-player + foliage-skip surface snap over `SurfaceProbe`); a co-op zone-hold objective timer ([`instance/zone/`](zc-instance/src/main/java/com/ziggfreed/common/instance/zone/CLAUDE.md) - `ZoneHoldTimer`, the "required group holds a zone TOGETHER for X seconds, reset on break" state machine, for extraction pads / capture points / king-of-the-hill; PURE logic); a damage-cause index cache ([`util/DamageCauseCache`](zc-core/src/main/java/com/ziggfreed/common/util/CLAUDE.md)); and a `RoundMetadata` outbound-event envelope ([`instance/metadata/`](zc-instance/src/main/java/com/ziggfreed/common/instance/metadata/CLAUDE.md)). **Kweebec Nightmare is the exemplar consumer** (hunter on-hit slow + bonus damage + proximity-stacking + enrage; asset-driven spawn triggers near players on shrine-light + at corruption milestones; a multi-phase reskinned boss) - the SAME primitives back any future MMO dungeon/raid. Design + verified engine seams + in-game spikes: [[kweebec-difficulty-spawn-design]].

It has **zero MMO dependency and NO Perfect Utils coupling** - the only dependency is the Hytale server jar. The plugin registers ziggfreed-common's own framework asset stores (`FrameworkAssetRegistrar.registerAll` in `setup()`) AND wires the library's own runtime: the shared progression runtime and its five producer systems, the `/zigprogress` and `/zigflair` command families, the commerce engines, the NPC placement engine, the placed-block ledger, the per-player flair and progress components, and the dialogue-memory backend. The stateless static primitives still register nothing - a consumer calls them directly.

> **PARADIGM (remember this): a generic, mod-agnostic Hytale primitive belongs HERE, not duplicated in a consumer mod.** When a sibling mod (Kweebec Nightmare, a future minigame, the MMO) needs a reusable engine wrapper - 3D sound, camera, an asset-index resolve, a HUD/toast helper, a worldgen/terrain query, etc. - add it to `ziggfreed-common` and consume it; do NOT reimplement it per mod. New primitive = a new file here (+ its package router), then rebuild the jar consumers pin. A consumer keeping only the mod-SPECIFIC policy (round logic, content config) is correct; the reusable engine call underneath is what gets lifted. (Latest example: `world/SurfaceProbe`, lifted out of Kweebec's `ArenaBuilder` so any mod can floor-snap onto procedural terrain.)

> **PARADIGM (the goal is no-Java pack authoring): consumer CONTENT + display CONFIG belongs in `/Server/` pack JSON, NEVER Java-baked.** Anything a pack author should be able to create or tune without code - dialogues, the queue-mode cards, leaderboard tabs/columns, party knobs, reward lists, queue policy - is a codec asset loaded from `Server/<Mod>/<Type>/` (Pattern A structured codec - the codec IS the schema - or, for a legacy type not yet converted, an `extends`/`params` DSL), resolved `defaults < pack < owner`. The library ships NEUTRAL DEFAULT content only where a bare server would otherwise get nothing (the seven `FeedbackMoments` files, the `DialogueOptionTheme` looks), as ordinary JSON in its own jar's asset pack that a consumer overrides BY ID (a later-loaded pack's same-id file wins, and a consumer that lists this library as a dependency loads after it); everything else is consumer pack JSON. **A CONTENT-like default ships as a JSON asset in the owning jar; a STRUCTURAL default stays code.** Content-like = anything a player reads, hears or sees that an author would want to word or tune (wording, colours, sounds, icons, item lists); structural = the mechanism's own posture when nobody registered anything ("a gate is open when nobody registered one", an empty catalogue, an in-memory store, a strict anti-exploit ledger a consumer installs its own policy over). A Java constant list / hardcoded JSON string that represents content is a smell - lift it to an asset. **Common OWNS its framework stores** (one class = one registrant; a consumer must NOT re-register them - it authors JSON into the path and reads the resolved config singleton back). The registered framework asset types ([`asset/FrameworkAssetRegistrar`](src/main/java/com/ziggfreed/common/asset/FrameworkAssetRegistrar.java), all via the [`asset/AbstractKeyedAssetConfig`](zc-core/src/main/java/com/ziggfreed/common/asset/AbstractKeyedAssetConfig.java) fold + the Pattern-B [`asset/AbstractRawJsonAsset`](zc-core/src/main/java/com/ziggfreed/common/asset/AbstractRawJsonAsset.java) base): **Dialogues** ([`dialogue/asset/`](zc-dialogue/src/main/java/com/ziggfreed/common/dialogue/asset), Pattern A: the conversation body decodes through the process-wide `DialogueTypeTable` codec graph, with native `Parent` inheritance and a per-screen merge; a base is an ordinary file marked `Abstract`), **Instances** ([`instance/preset/InstancePresetAsset`](zc-instance/src/main/java/com/ziggfreed/common/instance/preset/InstancePresetAsset.java)), **Lootables** (`loot/LootableAsset`), **Bosses** (`instance/encounter/MultiPhaseBossAsset`), **BandedEffects** (`instance/effect/BandedEffectAsset` -> `EffectBandLadder`), **EncounterRules** (`instance/encounter/EncounterRuleAsset`, free-string Trigger), **PrefabPlacements** (`world/WeightedPrefabPlacementAsset`), **Leaderboard** (`instance/leaderboard/LeaderboardLayoutAsset`), **Arenas** (instance/arena/ArenaDefinitionAsset), **Party** (`party/PartySettingsAsset`), **FeedbackMoments** ([`feedback/moment/`](zc-presentation/src/main/java/com/ziggfreed/common/feedback/moment/FeedbackMomentAsset.java), Pattern A: the file NAME is the moment id (`Quest_Completed`, `Achievement_Unlocked`, ...) and its four independent groups say what the server does - a toast, a server banner, a jingle, a command - with `Variants` letting one file word several cases of the same moment; the library SHIPS a neutral default file for each of the seven moments its own engines announce, and a consumer's same-id file replaces it by pack order), **NpcIdentities** ([`npc/NpcIdentityAsset`](zc-dialogue/src/main/java/com/ziggfreed/common/npc/CLAUDE.md) - the OVERLAY on top of the naming convention, for aliases / one character across two roles / a rename; most NPCs need no file, since a character IS its role by default), **Quests** + **QuestGenerators** ([`quest/asset/`](zc-progression/src/main/java/com/ziggfreed/common/quest/asset/CLAUDE.md), native `Parent` inheritance with a per-objective-id merge; a generator writes a whole family as ordinary `Parent` children, and a consumer folds the WHOLE store into its own engine via `QuestAssetStore.resolveAll(enumerators)`), **Achievements** ([`achievement/asset/`](zc-progression/src/main/java/com/ziggfreed/common/achievement/asset/CLAUDE.md), native `Parent` inheritance; `Criteria` is a KEYED MAP merged per criterion id under `Parent` (via `InheritMapCodec`), and the key is what progress is stored under, so renaming one starts that criterion over while adding, removing or reordering entries moves nobody's progress; folded via `AchievementAssetStore.resolveAll()`), **AchievementCategories** + **AchievementMilestones** ([`achievement/asset/`](zc-progression/src/main/java/com/ziggfreed/common/achievement/asset), the display taxonomy and the points ladder), the COMMERCE family **Currencies** / **Shops** / **ShopPools** / **ShopEntries** / **ShopEntryGenerators** / **Boards** / **Bounties** (`zc-commerce`, one wallet / storefront / rotating shelf / offer / fan-out / notice board / contract per file), plus **DialogueFragments** (`dialogue/asset/DialogueFragmentAsset`), **DialogueOptionTheme** (`dialogue/asset/DialogueOptionThemeAsset`), **RollPools** (`loot/stamp/RollPoolAsset`), **RewardKinds** (`loot/reward/RewardKindAsset`), **NpcPlacements** (`npc/placement/asset/NpcPlacementAsset`) **ObjectiveKinds** (`progress/asset/ObjectiveKindAsset` - one KIND of quest or achievement step per file: what it counts and how, what its target names, and how it reads and looks; merged leaf by leaf over whatever code registered the same id by `ObjectiveKindFold`, so a file states one fact and leaves the rest alone, and the library ships one for each of the 23 built-in kinds) and **Factors** (`factor/DerivedFactorAsset`) - **Encounters** (`encounter/asset/EncounterBindingAsset`, what the server owes for a native encounter script: credit, scale, loot, feedback, discovery; owner layer `mods/ziggfreedcommon/encounters.json`) and **EncounterParticipation** (`encounter/asset/EncounterParticipationAsset`, how credit for a fight is shared, matched per subject and per world; owner layer `mods/ziggfreedcommon/encounter-participation.json`) - thirty-four stores in all. A consumer reading any of these must do so LAZILY / post-load (the configs are populated by `LoadedAssetsEvent` AFTER plugin `setup()`). Plan + remaining consumption work: [[ziggfreed-common-asset-driven-configs]].

## Build

Gradle runs via PowerShell (Java 25). Self-contained `build.ps1` builds + installs:
```powershell
cd 'D:\dev\business\hyMMO\additional-mods\ziggfreed-common'; .\build.ps1
.\build.ps1 -Install:$false     # build only
.\build.ps1 -ModsDir <path>     # explicit install target (else $env:HYTALE_MODS_DIR)
```
Produces `build/libs/ZiggfreedCommon-<version>.jar` and copies the runtime jar (never `-sources`/`-javadoc`) into the Hytale `Mods/` folder when `HYTALE_MODS_DIR` is set. `.\gradlew.bat build` works too. The Hytale server jar is referenced `compileOnly` (path in `gradle.properties`: `hytaleHome`/`patchline`/`game_build`) and NEVER bundled; jsr305 is `api` on each module (the `@Nonnull`/`@Nullable` annotations must resolve for consumers that compile against this jar) but is deliberately NOT bundled into it.

## Modules (build-time only - one jar ships)

The library is built as FOURTEEN Gradle modules plus a thin wiring root, all sharing the ONE package root `com.ziggfreed.common`. **A module boundary is a build-time concern only**: the module jars are merged back into the single `ZiggfreedCommon-<version>.jar` a server loads, so no consumer import, class name, `manifest.json`, or build script changes because of it. Only the aggregate is installable - the per-module jars in `zc-*/build/libs` are compile inputs, carry no `manifest.json`, and are NOT loadable mods on their own (the shipped `.ui` pack references across module lines, e.g. a dialogue page importing the shared frames). The point of the split is that a module reaches only what its `build.gradle` declares, so an accidental cross-domain edge fails the build instead of quietly welding two domains together.

**Every inter-module edge is `implementation`, never `api`.** An `api` edge leaks its own dependencies onto every downstream module's compile classpath, which is exactly the isolation this table claims to have and would not have. Restore `api` on a single edge only when a module's PUBLIC SIGNATURE genuinely re-exports another module's type, and name that type in a trailing comment. One such re-export exists today and deliberately stays `implementation`: `zc-dialogue`'s `DialogueCondition.World#getSelector` returns a `world.WorldSelector`, and the one module depending on `zc-dialogue` reads NPC identity and never calls it, so no `world.WorldSelector` has to resolve on its compile classpath. The day a dependent DOES call it, that dependent declares its own `zc-world` edge, or this edge becomes `api`. (`jsr305` stays `api` in `gradle/zc-module.gradle` - consumers compiling against the aggregate must resolve the annotations.)

| Module | Packages | Depends on | Ships `.ui` |
|---|---|---|---|
| `zc-core` | `CommonLog`, `util`, `codec`, `command`, `validation`, `registry`, `factor` (the model + registry + condition), `match`, `subject`, `counter`, `time`, `i18n`, `icon`, `inventory`, `health`, `stats.StackStats`, `cast.WorldEvictors`, `asset` (the base classes), `text` (`ContentTextAsset`) | - | - |
| `zc-presentation` | `ui`, `sound`, `camera`, `feedback` (incl. `feedback.moment`, the authored-feedback engine) | core, loot (one seam: `ui/toast/RewardToastLines`, the chip-to-toast-rows bridge) | yes (the shared frames + buttons + toast + form rows) |
| `zc-cast` | `cast` (minus `WorldEvictors`), `interaction` | core | - |
| `zc-entity` | `entity`, `stats` (minus `StackStats`), `factor` (the portable `hytale:` standard library) | core | - |
| `zc-effects` | `effect`, `instance.effect` | core | - |
| `zc-loot` | `loot` (the ONE loot core: `Roll`/evaluator/engine + the `Lootable` asset), `loot.stamp` (rolling stats onto an item + the budget math), `loot.reward` (the reward VOCABULARY every payout site shares), `instance.reward` | core | - |
| `zc-world` | `world`, `world.placed`, `world.pattern`, `world.record`, `world.stash`, `worldmap` | core, entity | - |
| `zc-dialogue` | `dialogue` (incl. `dialogue.quest`), `npc` (incl. `npc.placement`) | core, presentation, world, progression | yes (the dialogue page + option row) |
| `zc-progression` | `progress` (the shared lifecycle cores, plus `progress.asset` the shared authoring groups and `progress.gate` the shared requirement model), `quest` + `quest.asset` + `quest.event` (the quest engine, its authoring layer + generator, its events), `achievement` + `achievement.asset` + `achievement.event` (the always-on peer engine) | core, loot | - |
| `zc-objectives` | `objectives` (the standalone progression runtime: `objectives.runtime` / `.store` / `.producer` / `.book` / `.questlist` / `.hud` / `.admin` (the progression-systems admin page over the registered `SystemSwitches`) / `.dialogue` (the `ActiveObjective` dialogue header source), plus `objectives.command`: `/zigprogress`, the admin family over the runtime - quest, achievement and memory groups, MMO-agnostic; and `objectives.flair`: the `Flair` reward kind, the `FlairUnlocks` write facade over zc-entity's `ZigFlairComponent`, the `ZigFlairChangedEvent` native event and `/zigflair grant|revoke|list`) | core, loot, progression, presentation, cast, entity, dialogue (NPC identity, and `DialogueMemories` for the memory verbs), world (`world.placed` only, so the break/pickup producers refuse a block the breaker placed), instance (the `InstanceRoundCompletedEvent` seam only, so a finished minigame round feeds the same engines every other moment feeds) | yes (the objective book page + its row family) |
| `zc-instance` | the remaining `instance.*`, `lobby`, `party` | core, presentation, loot | yes (play / results / leaderboard / party pages) |
| `zc-scaling` | `scaling` | - (no engine types at all) | - |
| `zc-commerce` | `currency` (definitions + the item-or-counter dispatch + the balance engine), `cost` (the price model + the check/drain/refund engine + receipts), `rotation` (the deterministic rotating-pool primitive), `shop` (storefront/pool/entry assets + the purchase engine), `board` (board + bounty assets + the rotating quest-view engine), `commerce` (the persisted-state seam + the per-player component behind it) + `commerce.fold` (the JOIN: authored content to engine values, the catalogs, the owner-file readers, the `Currency` reward kind, the `Shop`/`Board` destinations, the content audit) + `commerce.command` (`/zigcommerce`, the library's first command family) | core, loot, progression, presentation, world | yes (shop page + board page) |
| `zc-encounter` | `encounter` (the boss framework over the engine's own `EncounterManager`: `encounter.asset` the binding row + participation rule types, `encounter.signal` the `zc:` grammar and the `WorldEventSignal` bridge, `encounter.run` the codec-less run component, the live table, the lifecycle story, subjects, membership, scaling, the spawner and the runtime facade, `encounter.ledger` the pure participation ledger, `encounter.system` the attach/remove, death, damage-inspect and tick systems, `encounter.event` the six native events, `encounter.seam` the four root-filled seams, `encounter.payout` loot / feedback / discovery, `encounter.types` the five registered encounter Types, `encounter.validate` the script scan + validator + audit + boot self-test, `encounter.command` `/zigencounter`) | core, entity, world, effects, loot, presentation, scaling (never instance, objectives, progression, dialogue or commerce: `EncounterEdgeTest`) | - |
| **(root)** | `ZiggfreedCommonPlugin`, `asset/FrameworkAssetRegistrar`, `reward/EffectRewardKind` (the cross-domain reward kind only the root can see both ends of) | all fourteen | - |

**`.ui` files are a RUNTIME dependency Gradle cannot see.** A page's `.ui` imports the shared `ZigButtons.ui` / `ZigFrames.ui` from zc-presentation by path, and the build never looks inside a `.ui` file. So any module marked in the last column requires zc-presentation AT RUNTIME even where it has no compile edge - harmless while one aggregate jar ships, and the thing to remember the day anything is split or shipped separately. Fill in that column for every new module; the build will never tell you.

### zc-core admission rule

zc-core is the gravity well this whole split exists to resist: each package is individually reasonable there, and collectively that is exactly how the pre-split monolith formed. **A package enters zc-core only if (i) TWO OR MORE modules need it and (ii) it carries no domain vocabulary - and the row below must NAME the consumers.** A package that cannot name two is a package that belongs in the module that wanted it.

| zc-core package | Modules naming it | Standing |
|---|---|---|
| `CommonLog`, `util` | zc-presentation, zc-cast, zc-entity, zc-world, zc-dialogue, zc-progression, zc-instance, zc-commerce | admitted. `util/PeriodMath` inside it is named by TWO on its own account: zc-progression's `RepeatPeriod` (a quest's repeat window) and zc-commerce's `RotationSpec` (a rotating pool's cadence) both index the same UTC `floorDiv` arithmetic through it, so "which window is this instant in" has ONE authority rather than two implementations that agree until one of them is fixed |
| `time` | zc-commerce (the authored `{Days,Hours,Minutes,Seconds}` span on a rotation cadence). ONE library module today | admitted UNDER THE CARVE-OUT, like `counter` and `match`, and the row says so. It stays because it is the primitive floor's shape exactly - an authorable span with no engine type and no domain vocabulary - and because any consumer authoring a duration wants the same four leaves rather than a unit-suffixed string of its own. If a second library module names it, move this row up |
| `asset` (bases) | zc-effects, zc-loot, zc-world, zc-dialogue, zc-progression, zc-instance | admitted |
| `cast.WorldEvictors` | zc-entity, zc-world, zc-dialogue | admitted (split package, see below) |
| `codec` | zc-entity, zc-dialogue, zc-progression, zc-world | admitted |
| `validation` | zc-world, zc-dialogue, zc-progression | admitted |
| `factor` (model / registry / condition) | zc-dialogue, zc-progression | admitted |
| `registry` | zc-dialogue, zc-progression | admitted |
| `subject` | zc-progression (both engines), zc-loot (a reward handler is handed a `Subject`), zc-core's own `counter` | admitted as the ONE identity vocabulary under every engine |
| `counter` | ONE library module (zc-instance, whose `LeaderboardEntry` holds a `CounterMap`) plus the MMO's `StatisticsComponent`, which uses all three types | admitted UNDER THE CARVE-OUT, not the two-module rule: it is one module short and the row says so rather than implying otherwise. It stays because it is the primitive floor's shape exactly - named long tallies, no engine type, no domain vocabulary - and any consumer keeping per-subject lifetime totals builds a `Counters` over its own store. If it ever gains a second library module, move this row up; if zc-instance ever drops it, it belongs in the MMO. See `stats/CLAUDE.md` for why `counter/` and `stats/` never merge |
| `match` | ONE library module (zc-world, whose `WorldNameMatcher.Pattern` IS a `NamePattern` and whose `MatchRank` IS a `NameMatchRank` plus one world-specific rung) plus two sibling mods: the MMO's bonus-drop `When.Match` (pattern + ladder both) and mmo-mob-scaling's rule matching | admitted UNDER THE CARVE-OUT, like `counter`: one library module short of the rule, and the row says so. It stays because it is the primitive floor's shape exactly - a pure grammar and a total ordering, no engine type, no domain vocabulary - and because the home has to be BELOW both zc-world and zc-loot for a loot-side trigger key to ever speak the same grammar, which zc-world could never offer it (`zc-loot` has no edge to zc-world and never will). A second grammar for "which of these names does this one mean?" is exactly the drift this module exists to prevent; if a library module above zc-world names it, move this row up |
| `stats.StackStats` | zc-entity, zc-loot (the default stamper writes it) | admitted as a pure item-metadata value record (split package, see below) |
| `text.ContentTextAsset` | zc-core's own `factor` (`DerivedFactorAsset`'s naming group), zc-commerce (currency/shop/board/bounty `Text`), zc-progression (quest/achievement `Text`) | admitted. NOT a split package: the file lives here alone (moved out of zc-progression's `progress.asset`, which shared the package but not the module), so every consumer above this module reaches it through the ordinary dependency edge rather than a same-FQN coincidence |
| `inventory` | SPLIT, and worth reading as three parts. The ACCESSOR part is `PlayerAccess`, the one non-deprecated replacement for the engine's deprecated `Inventory`/`Player` section accessors: read by this module's own `InventoryGrant`, by rpg-stations and by the MMO. The grant/spend part is properly shared too: `InventoryUtil` in zc-loot + zc-objectives + kweebec, `InventoryGrant` in zc-loot + rpg-stations + the MMO. The SNAPSHOT part (`InventorySnapshot`, `InventorySections`, `InventorySnapshotStore`, `InventoryStripPolicy`) has one consumer, kweebec's round inventory guard, and `InventorySections` has none | the accessor and grant parts are admitted outright. `PlayerAccess` additionally has to live at this depth: zc-core sits below zc-entity, so a consumer needing only the accessor shape never pulls in the puppet/performer stack. The snapshot part is on notice: one consumer, and `InventorySnapshotStore` describes itself in that consumer's terms, which is the domain vocabulary this module is supposed to carry none of. Either a second consumer arrives or it moves into kweebec |
| `icon` | `IconSpec` in zc-loot (a `RewardChip`'s picture), zc-progression (an objective kind's), zc-presentation (the renderer that paints one) and zc-commerce, plus mmo-mob-scaling, which is where it was lifted from; `Portraits` in zc-progression and mmo-mob-scaling | admitted, and it had to land BELOW zc-presentation rather than beside the renderer: zc-progression describes what a step looks like and has no edge to presentation, so a picture the engine layer can name must be reachable from here. It carries no drawing and no engine type - two nullable strings and a codec - so the module that paints one and the module that merely names one both reach the same value |
| `command`, `i18n`, `health` | `command` - `CommandRunner` in zc-loot plus the MMO and rpg-stations, `AbstractTargetPlayerCommand` in zc-commerce and zc-objectives (the shared per-player admin-verb resolution walk, generic over what a family builds from a resolved online player); `i18n` split by class - `ContentKeys` in zc-dialogue, zc-commerce, zc-loot, zc-presentation, zc-progression and zc-objectives (no consumer registers anything: it attributes a bare authored key against the server's loaded lang catalogue), `Msg` in zc-objectives, zc-commerce, zc-loot, zc-dialogue and rpg-stations, `NativeNames` in zc-loot, `GeneratedLangPack` in the MMO alone; `health` in no library module at all, only kweebec and mmo-mob-scaling | admitted. `i18n` now clears the two-module rule outright on `ContentKeys` alone, and it HAD to land here rather than in any one domain: the authored-key seam sits under dialogue, commerce and objectives at once, and a second answer to "what is this key really called" is exactly the drift that let board, shop and contract titles ship as raw keys. `command/AbstractTargetPlayerCommand` clears the two-module rule too, on the same two families. `CommandRunner`, `health` and `i18n/GeneratedLangPack` still lean on the primitive-floor carve-out - each has a single caller outside this module, so treat their shapes as unsettled and expect the second consumer to want changes |

### The five split packages

Five packages deliberately span a module line, which a flat merged jar makes free. Each needs its reason recorded, or the exception quietly becomes the rule:

- **`com.ziggfreed.common`** - `CommonLog` and `LibraryOwner` in core, the plugin in the root. The logger handle and the owner-attribution constant must both be reachable from every module; the plugin entry point must not be.
- **`com.ziggfreed.common.asset`** - the reusable bases in core, `FrameworkAssetRegistrar` in the root. The registrar reaches into six domains to register their stores and would cycle from anywhere else.
- **`com.ziggfreed.common.factor`** - the model / registry / condition in core so any module can gate on a factor; the portable `hytale:` standard library in zc-entity, because it reads engine entity and item data.
- **`com.ziggfreed.common.cast`** - `WorldEvictors` in core, the rest of the cast / interaction runtime in zc-cast. Every domain registers into the per-world eviction fan-out, and making three modules depend on the whole 38-class cast runtime for that one class defeats the guard. Keeping the FQN meant the call sites across the consumer mods never moved.
- **`com.ziggfreed.common.stats`** - `StackStats` in core (a pure item-metadata value record: codecs, an `ItemStack`, nothing else), the ECS bridging in zc-entity. A module that stamps a stack should not have to drag puppets and equip triggers along.

**Adding a package**: put it in the module whose domain it belongs to (`git mv` the directory into `<module>/src/main/java/com/ziggfreed/common/`; its tests mirror into the same module). If it needs something the module does not already declare, add a ONE-WAY `implementation project(':zc-x')` edge, never a cycle. Shipped `/Server/` and `/Common/UI/` resources live in the module that owns the code reading them; `manifest.json` stays root-only, and no module may carry one. A genuinely cross-domain test (`asset/AssetCodecInitTest`, which touches every asset codec) stays in the ROOT project's test set rather than forcing edges onto a module.

**A seam that SHIPS is a seam that is FILLED (build-enforced).** An interface declared here ships with a production default in its own module or a root fill in the SAME change; a seam kept intentionally empty for the next caller is deleted, not kept. Every seam degrades silently to "does nothing" when unfilled, so a gap never fails a build and never fails a boot - it fails a player, later, invisibly (an achievement nothing gates, a list that names nothing, an NPC with nothing to say). `SeamsFilledTest` (root test set) walks the registration state after `ProgressionDefaults.register()` and names any progression seam still standing empty: a gate for BOTH engines, a text source, an offer provider, a claim table, a store. A seam the WIRING ROOT fills at plugin setup rather than one those defaults register (the dialogue memory backend) is out of that test's reach, so it REPORTS ON ITSELF from inside its own module the first time it has to fall back, once per process, through `SafeLog`: `DialogueMemories.persistentBackendOrWarn` names the missing `DialogueMemories.install(PersistentStore)` fill and what it costs the player, and `FirstClaims.store` names the boot-lifetime claim table and the `FirstClaims.install` that replaces it. Give a root-filled seam a report of that shape (pinned by a test in its own module), never a `SeamsFilledTest` assertion that cannot fail. `NpcOffersLivePathTest` (root test set too, for the same reason - it needs the registered defaults) walks the whole live offer path a giver's screen and a conversation's "have you anything for me" line both take. **Both of those tests leave the shared progression runtime CLEARED on teardown**, so a future root test that reads registration state must register what it needs in its own setup rather than inheriting whatever ran before it.

**The wiring root is REGISTRATION ONLY (build-enforced), and so is every module `*Bootstrap`.** The root is the one place an edge between any two modules is legal, so it will absorb every awkward dependency offered to it - and logic that settles there is logic no module can be tested or reasoned about without standing up the whole plugin. Most registration phases live in per-module bootstraps the root's `setup()` calls in one fixed order (`entity/EntityBootstrap`, `npc/NpcBootstrap`, `world/placed/PlacedBlockBootstrap`, `objectives/runtime/ProgressionBootstrap`, `objectives/dialogue/DialogueBootstrap`); only a phase no single module can see both ends of keeps its body in the root, pinned by a named line in the root's class javadoc. `RootRegistrationOnlyTest` (root test set) walks EVERY `.java` file under the root's `src/main/java` PLUS every `*Bootstrap.java` under any module's `src/main/java` - it discovers them rather than naming them, so a fourth root file or a new module's bootstrap cannot slip in unscanned (which also makes the `*Bootstrap` suffix load-bearing: name a setup-time registration class that way or the walk cannot see it) - and fails the build on a loop, a `switch` or an `else`. What passes: a try/catch guard, an `if` that tests for `null` or whose branch leaves at once by `return` or by `throw` (both are "refuse this input and get out"), and a null-defaulting ternary, including one wrapped across several lines. Escape hatch `// ROOT-LOGIC-OK: <reason>` with a real reason, on the line, inside the statement, or in the comment block above it. If the root has to DECIDE something, that decision belongs in a module behind a seam the root fills.

**The reverse-edge trap.** A module's position in the graph permanently forbids the edges above it. `zc-progression` sits below `zc-dialogue`, so it may never import dialogue, presentation or world - yet quests will want an NPC turn-in conversation, a results page, a waypoint and world-scoped objectives. `zc-loot` sits below both `zc-progression` and `zc-instance` and is more constrained still. **Every one of those needs is a SEAM the lower module declares and the wiring root or the consumer fills, never an import.** The first violation is not found until it is an unbreakable cycle three modules deep, mid-rewrite, so the rule is repeated in those modules' own routers.

### SubPlugins: not now, and the checklist for later

The `manifest.json` declares ONE identity (`Ziggfreed:ZiggfreedCommon`, one `Main`, one asset pack, no `Dependencies`), and that is a truthful statement of reality: the zc-* modules are compile-time slices with no independent lifecycle, so there is no runtime identity for the manifest to fail to express. **Do not add a `SubPlugins` block.** Every candidate engine ships EMPTY of content by design, so there is nothing to toggle; one jar means one classloader, so a "disabled" sub still links and merely never registers its stores (a silently half-initialised library instead of a clean failure); each sub would need its own required `Main`, splitting an indivisible boot sequence that already carries ordering constraints; and a sub-identity cannot carry its own asset pack (`AssetModule.registerPackFromJar` reads the jar's TOP-LEVEL manifest and registers one pack per jar).

Any ONE of these flips that answer:

- A domain acquires a genuinely optional **runtime cost**: a per-tick system, a registered ECS component/system, or a global event listener.
- A domain needs its **own `Dependencies`** - a sub-manifest can carry a different dependency map than its parent, which is the one thing the parent manifest structurally cannot express.
- A domain needs its own **`ServerVersion`** range or its own **`Permissions`** block (both are per-manifest).
- A third party credibly asks to depend on one domain **without** the rest's runtime.
- A domain becomes independently **releasable** with its own version cadence visible to consumers.

**Precondition before ever pulling the trigger**: the candidate module must first have a self-contained `setup()` / `shutdown()` that is SAFE TO SKIP, and every dependent must degrade gracefully when it is absent. Neither is true of any module today.

## Consuming this mod

A consumer mod adds ziggfreed-common as a `compileOnly files(...)` dependency (the local jar path until a remote + submodule gitlink exist) and lists it in its `manifest.json` `Dependencies` (or `OptionalDependencies`) so the server loads it first. NEVER bundle this jar into a consumer (double-loading engine-touching classes under two classloaders breaks identity). All primitives are static, config-free, and reusable as-is.

## Layout

**Read this as a map of PACKAGES, not of paths.** Every package below lives inside exactly one
`zc-*` module - which one is the Modules table above, and every link in the Architecture section
below carries the real path. The repository root holds almost nothing: three java files, one
`manifest.json`, and the build scripts.

```
settings.gradle / gradle.properties / build.gradle    fourteen zc-* modules + the aggregating root (see Modules)
gradle/zc-module.gradle                                the shared build convention every zc-* module applies
build.ps1                                              build + auto-install
src/main/resources/manifest.json                       root-only, and the ONLY resource at the root. Group:Ziggfreed,
                                                        ServerVersion ">=0.6.0-pre.13 <0.7.0", IncludesAssetPack:true, NO Dependencies
src/main/java/com/ziggfreed/common/                    the root's whole source: ZiggfreedCommonPlugin (JavaPlugin entry -
                                                        LOGGER, setup/shutdown), asset/FrameworkAssetRegistrar (registers every
                                                        framework store), reward/EffectRewardKind. Registration only, build-enforced
SHIPPED ASSETS live in the modules that own the code reading them, never at the root:
  zc-presentation  Common/UI/Custom/Common/ZigButtons.ui + ZigFrames.ui (neutral, base-game textures),
                    Pages/ZigToast.ui, Pages/ZigListRow.ui (generic CRUD row), the five ZigForm*Row.ui templates,
                    Server/ZiggfreedCommon/FeedbackMoments/<moment id>.json (the seven neutral default moments)
                    + Server/Languages/<locale>/ziggfreedcommon.feedback.lang (9 locales)
  zc-dialogue      Pages/ZigDialoguePage.ui + ZigDialogueOptionRow.ui, the DialogueOptionTheme JSONs
  zc-instance      the play / results / leaderboard / party pages
  zc-objectives    the objective book page + its row family, the NPC quest page + its rows, the tracked-quest HUD
                    (Hud/ZigQuestTracker.ui + three native textures) + the tracked side-panel row
  zc-core          Server/Languages/<locale>/ziggfreedcommon.fmt.lang (9 locales)
  zc-commerce      Server/Languages/<locale>/ziggfreedcommon.commerce.admin.lang (9 locales)
  zc-objectives    Server/Languages/<locale>/ziggfreedcommon.progression.admin.lang (9 locales, the /zigprogress family) beside ziggfreedcommon.progression.lang + items.lang
  zc-objectives    Server/Languages/<locale>/ziggfreedcommon.flair.lang + ziggfreedcommon.flair.admin.lang (9 locales each: the Flair_Unlocked toast line, and the /zigflair family)
THE PACKAGES (one module each - see the Modules table for which):
  sound/                                               Sound3D (3D SoundEvent playback)
  camera/                                              CameraShakeService + ServerCameraService
  cast/ (+ cast.step/)                                 reusable cast/ability RUNTIME layer (WorldEvictors is a zc-core file under this package): the generic step-dispatch kernel (StepHandler/StepRegistry/StepSemantics/CastKernel) + OnHitRegistry + HitContext/HitAction/HitResolver + ObserverRegistry + CastParams + ArmedStateStore + RaycastTargeting/BlockRaystep + per-world tick partition (WorldEvictors/WorldKeyedQueues/AbstractWorldFrameSystem, which rides the engine's own once-per-world-per-tick dispatch) + ModelParticleService - see its router
  util/                                                AssetIndexCache + NumberFormatter + CommandExecutor + HostilityUtil + EntityIdentifierUtil + DamageCauseCache (memoized DamageCause index) + JsonTreeUtil/JsonOverrideWriter (JSON merge + partial owner-file write-back)
  validation/                                          the ONE audit-finding vocabulary every content validator reports through (Finding/Severity/ValidationReport) - see its router
  command/                                             CommandRunner (the authored-command primitive: map-driven placeholders + the /give --quantity fix + a guarded Consumer<String> failure sink over util/CommandExecutor) - see its router
  registry/                                            RegistryLedger (the shared open-registry bookkeeping engine: normalized ids, owner attribution, identity-compared warn-once overwrite, failure counting) - see its router
  factor/                                              the shared namespaced factor + condition vocabulary (FactorContext/FactorProvider/FactorRegistry/FactorCondition/FactorConditions in zc-core, the portable hytale: HytaleFactors standard library in zc-entity beside entity/HeldItemUtil - stats, tags, held items, and hytale:permission; plus the ziggfreedcommon: progression readings quest_known/quest_completed/quest_completions/achievement_earned/achievement_points contributed by zc-progression's runtime; hytale:mod_installed (ModFactors) answers for a code plugin AND an asset-only pack); ONE gate leaf every engine reuses, null-sentinel fail-closed throughout - see its router
  event/                                               NativeEventSeam (the one guarded native-event dispatcher every IEvent<Void> family fires through: hasListener-gated, built lazily, synchronous on the calling thread, whole-body guarded; one instance per family with a publishTo seam for a bus-less host or a test - QuestEvents and FlairEvents both sit on one)
  instance/effect/ encounter/ metadata/ zone/         reusable ENCOUNTER framework: EntityEffectService/EffectBandLadder (timed/banded effects), SpawnRoster/EncounterDirector (weighted waves + gating), RoundMetadata (outbound envelope), ZoneHoldTimer (co-op hold-a-zone objective) - see the framework blockquote above
  encounter/ (+ asset/ signal/ run/ ledger/ system/ event/ seam/ payout/ types/ validate/ command/)  the BOSS framework over the engine's own EncounterManager (zc-encounter): the fight is a native script the engine runs, this module gives it native IEvents off its zc: signals, a (runId, player) participation ledger fed by an observe-only damage system, party/power health scaling, share-scaled loot + feedback + a map marker, five registered encounter Types (ZigGrant/ZigFeedback/ZigScaleTarget/ZigMembers/ZigFactor), the Zc_Encounter_Base Abstract + three macros + the spawnable Zc_Encounter_Example, and /zigencounter - see zc-encounter/CLAUDE.md
  instance/match/ arena/ result/ preset/ reward/ play/ + leaderboard/  the instance-experience layer: match rules/verdict, ArenaDefinitionAsset, MatchResult+ResultsPage, InstancePresetAsset, InstanceReward+DeferredRewards, PlayModePage, Leaderboard+LeaderboardPage - see the instance-experience blockquote
  asset/                                               generic Pattern-A registrar lift + the framework asset stores (FrameworkAssetRegistrar/AbstractKeyedAssetConfig/AbstractRawJsonAsset)
  entity/                                              PlayerModelService (player model swap) + PlayerPuppetService (skinned-puppet spawn/despawn/animate + scale self-hide/reveal) + ItemPropEntityService (networked item-prop entity spawn/despawn) + PuppetNav (bounded A* over CollisionModule) + performer/ (StationPerformer contract: HolderPerformer/NpcRolePerformer backends, PerformerIdentityComponent, PerformerReconciler) + flair/ (`ZigFlairComponent`, the per-player unlocked-flair ECS record the library registers and attaches at connect; a consumer peeks it, never re-derives it; a flair id carrying `|` or `:` is refused at grant and at the write path, since the save format reserves both; the WRITE path is zc-objectives' `objectives/flair/FlairUnlocks`, which fires `ZigFlairChangedEvent` and the `Flair_Unlocked` toast on every real change) - see its router
  stats/                                               unified per-stack + tag entity-stats bridge: StackStats (per-stack stat/enhancement record, a zc-core file under this package) + EquipStatBridge (held/armor/offhand -> native EntityStatMap modifiers, bridgedSum) + StatMirror + StatChannelAudit - see its router
  interaction/                                         the GENERIC interaction-composition framework: NativeChainFire (fire a named native RootInteraction chain by id on an entity - initChain+queueExecuteChain, fail-closed, +a decorator overload) + NativeInputGate (read whether a root can currently do anything, off its own opening conditions) + ChainWalker (a cycle-guarded chain walk) - see its router
  interaction/type/                                    custom interaction-Type registration plumbing (InteractionTypeSpec/InteractionTypes/InteractionCtx/InteractionOutcome) - see its router
  interaction/param/                                   the fire-time parameter-fold seam (CastScope/CastScopes/ParamFold/ParamSlot) - see its router
  interaction/target/                                  the server-authoritative targeting engine (TargetQuery/TargetSweep/LineOfSight) - see its router
  effect/                                              NativeEffectUtil (apply/remove a native EntityEffect by id: asset-authoritative + duration-override addEffect overloads, fail-closed) + AppliedEffectTracker (session-scoped tracked-set, removeAll strips everything a session applied) - see its router
  lobby/                                               the matchmaking lobby (MatchmakingQueue/LobbyService/RoundLauncher) - see its router
  npc/                                                 generic NPC dialogue-open action + auto-spawn/placement (ActionOpenDialogue/NpcSpawnService) - see its router
  party/                                               searchable-invite party (Party/PartyService + PartyInvitePage + PartySettingsAsset) - see its router
  inventory/                                           PlayerAccess (the shared non-deprecated Player/Inventory section-accessor replacements) + InventoryUtil (give/count/take/spend a custom resource item across the combined inventory) + InventoryGrant (hotbar-first-then-backpack-then-fallback single-stack GRANT ordering)
  health/                                              HealthUtil (native EntityStats heal: full-heal / heal-by-amount / full-restore + Store/Ref & Holder scaleMaxHealth)
  scaling/                                             generic difficulty-scaling engine (ScalingContext/ScalingEngine/PowerAggregation/AggregationMode) - domain-free fold+clamp, see its router
  feedback/ (+ feedback.moment/)                       Notify + EventTitles (styled NotificationUtil / EventTitleUtil wrappers) + the authored-feedback moment engine (FeedbackMomentAsset/Config/FeedbackEngine, shipping the seven neutral default moment files) + PickupMimic (native-pickup-mimic notifier: item-icon Notification + SFX_Player_Pickup_Item, honoring ShowItemPickupNotifications)
  ui/                                                  CustomHudHelper (HUD register / strip / restore) + SettingsUiUtil (settings-form binding helper) + form/ (the generic settings-form engine: FieldSpec + SettingsForm) + hud/ (KeyedCustomHud the keyed in-world HUD base + HudPosition the lifted layout value - see its router) + toast/ (lifted transport-agnostic in-page toast engine + ZigToast.ui) + rows/ (fixed-slot icon + rich-text LEDGER row: SummaryRow + SummaryRowRenderer)
  world/                                               WORLD TARGETING (the WorldSelector `Where` group / MatchRank / WhereValidator / WorldIdentity - a world is named by what it is CALLED or by the gameplay config it runs) + BlockOps (single-block read/write over the engine's current, non-deprecated block surface) + SurfaceProbe (top-solid-Y column probe -> floor-snap) + SpawnPlacement (ring/near-player runtime spawn positions, foliage-skip surface snap)
  world/pattern/                                       generic structure-pattern matching (BlockPattern<P> compile/variants over opaque cell payloads, BlockReader/CellPredicate seams, PatternIndex candidates + bounding radius) - see world/CLAUDE.md
  world/record/                                        BlockRecordSection<T> (the generic per-block keyed-record Component<ChunkStore> over a caller-supplied payload codec; register once, act through the typed Handle) - see world/CLAUDE.md
  world/stash/                                         BlockStash/StashPile (what a block position holds for players: piles, insertion-ordered items, one metadata-intact ItemStack, game-time clocks) behind the BlockStashes shared store, registered by BlockStashBootstrap - see world/CLAUDE.md
  worldmap/                                            WorldMapMarkers (world map / compass POI markers: global + per-player, over native WorldMapManager) + MapDiscovery (hidden-until-discovered POIs: ON_INTERACT/PROXIMITY x PER_PLAYER/SHARED) + the WAYPOINT mechanism (WaypointService/WaypointSnapshots over a WaypointTargetSource + WaypointPositionResolver pair)
  dialogue/                                            the generic branching NPC dialogue engine (one shared DialogueEngine + seams + page + validate) - see its router
  i18n/                                                Msg (mod-agnostic client-resolved Message factory: caller-prefixed tr/trNamed, raw, cat, bold/color wrappers) - see its router
  icon/                                                IconSpec (the ONE picture reference: an item id OR a Common-rooted texture path, as a nested codec group) + Portraits (where the client keeps its pre-rendered still of each creature, spelled once); ui/icon/IconRenderer is what paints one
  subject/                                             Subject (the ONE identity vocabulary every engine speaks: a stable id, a display name, and an OPAQUE consumer handle read back typed via handleAs)
  progress/                                            the shared lifecycle cores BOTH progression engines rest on: ObjectiveDef + ObjectiveKind/ObjectiveKindRegistry + MatchMode/ObjectiveMatch/ZoneRef + ObjectiveProgressState + ObjectiveIndex + DispatchOptions - see its router
  quest/ (+ quest.asset/, quest.event/)                the QUEST lifecycle over those cores (accept/track/hand-in/claim/cooldown) + the authoring layer + the outbound native events - see its router
  achievement/ (+ achievement.asset/, .event/)         the ALWAYS-ON peer lifecycle over the same cores (criteria/earn/collect/points/milestones/pins) + its authoring layer + its outbound native events - see its router
  counter/ (zc-core)                                   named long TALLIES per subject: CounterMap/CounterStore/Counters, category keys, no engine types - see its router
```

## Architecture (per-package routers carry the working detail)

Each domain package carries a nested `CLAUDE.md` router that loads when you touch that subtree. One line each:

- **[`sound/`](zc-presentation/src/main/java/com/ziggfreed/common/sound/CLAUDE.md)** - `Sound3D`: play a `SoundEvent` by id at a position OR an entity ref, with a `SoundCategory` + an optional per-listener `Predicate<Ref>`; index resolution through `AssetIndexCache`. World-thread.
- **[`camera/`](zc-presentation/src/main/java/com/ziggfreed/common/camera/CLAUDE.md)** - `CameraShakeService` (one-shot `CameraEffect` shake) + `ServerCameraService` (`SetServerCamera` top-down apply/reset). Packet-only; thread-safe writes.
- **[`util/`](zc-core/src/main/java/com/ziggfreed/common/util/CLAUDE.md)** - `AssetIndexCache` (the "cache ONLY a positive index" resolver), `NumberFormatter`, `CommandExecutor` (execute-only), `HostilityUtil`, `EntityIdentifierUtil`, `DamageCauseCache` (memoized `DamageCause` index, for identifying a custom damage cause in a `DamageEventSystem`).
- **[`inventory/`](zc-core/src/main/java/com/ziggfreed/common/inventory/CLAUDE.md)** - `PlayerAccess` (the ONE shared non-deprecated replacement for the engine's deprecated `Inventory#getStorage`/`getHotbar`/`getActiveHotbarItem`/`getCombinedBackpackStorageHotbar` and `Player#getPlayerRef`: a `(ComponentAccessor, Ref)` form plus a `Player` convenience overload per accessor) + `InventoryUtil` (give/count/take/spend a custom resource item by id across the combined inventory sections; world-thread, try-guarded).
- **[`health/`](zc-core/src/main/java/com/ziggfreed/common/health/CLAUDE.md)** - `HealthUtil` (native `EntityStats` heal: `fullHeal` / `heal(amount)` / `fullRestore`, via `EntityStatMap.addStatValue` + `DefaultEntityStatTypes` - the same API the MMO health-tick path uses; world-thread, try-guarded). Plus `scaleMaxHealth` in a `Store`/`Ref` form AND a ref-less `Holder` form (raise an entity's Health MAX inside a pre-add `onEntityAdd` spawn hook).
- **[`scaling/`](zc-scaling/src/main/java/com/ziggfreed/common/scaling/CLAUDE.md)** - the generic, domain-free difficulty-scaling engine: `ScalingContext` (base difficulty + a `double[]` of participant powers + `AggregationMode` + an opaque instance handle) folded by `PowerAggregation` and resolved by `ScalingEngine.resolve(ctx, bandWidth, minCap, maxCap)` into a band-clamped effective difficulty. Pure logic, zero engine coupling; the ONE engine open-world mob scaling and future instance scaling both call (only the context inputs differ). The Phase-0 lift for the MMO mob-scaling mod.
- **`feedback/`** - `Notify` (Default/Danger/Warning/Success toasts, plus `withIcon(playerRef, title, secondary, itemId)` for a two-line toast ILLUSTRATED by an item - quantity zero, so no "x1" badge and no client-side coalescing swallowing the second one's words, with an explicit-`NotificationStyle` overload for a notice carrying its own tone - and `itemKeyed(playerRef, title, secondary, itemId, quantity)` - the generic item-id-KEYED notification for "a client can stack this": rides the same `Item`-slot packet field a native pickup uses so consecutive sends sharing `itemId` coalesce client-side, quantity live-updating while `title` stays frozen text; Default style, no SFX/gate, try-guarded) + `EventTitles` (centered banner). All take a pre-built `Message`. Plus **[`PickupMimic`](zc-presentation/src/main/java/com/ziggfreed/common/feedback/PickupMimic.java)** (round-5, 2026-07-22) - the native-pickup-MIMIC notifier for a PROGRAMMATIC item grant that never went through a real ground-item pickup: `notify(ref, store, message, secondaryMessage, style, itemStack, position)` reproduces the engine's own `Player#notifyPickupItem` mechanism (the item-icon `Notification` packet via `ItemStack#toPacket()` + the `SFX_Player_Pickup_Item` cue, 3D at `position` when given else 2D) with the CALLER's own message/style, gated behind `ShowItemPickupNotifications` for the notification only (the SFX is never gated, matching the native method exactly); `notifyLikeNativePickup(ref, store, itemStack, position)` is the byte-exact convenience that delegates straight to `Player#notifyPickupItem` itself, so it can never drift from a genuine pickup. World-thread, try-guarded throughout; resolves the SFX index through `util.AssetIndexCache` (never the engine's own `@Deprecated(forRemoval = true)` `TempAssetIdUtil`).
- **[`feedback/moment/`](zc-presentation/src/main/java/com/ziggfreed/common/feedback/moment/FeedbackMomentAsset.java)** - the authored-feedback engine: what a server DOES when one lifecycle moment happens, as JSON at `Server/ZiggfreedCommon/FeedbackMoments/<moment id>.json`. Four independent groups (`Toast` / `Broadcast` / `Sound` / `Command`) over one reused `{Key, KeyArg, Args, Color}` line leaf (`KeyArg` reads the key from one of the moment's own values, so a per-content wording needs no file per piece of content); `Toast.Tone` is the closed `ToastKind` word (Info / Success / Reward / Warning / Error; unauthored Info) driving the in-page frame + headline AND the corner feed's native style together (the in-page toast built `silent()`, the `Sound` group staying the audio authority), and the fixed argument name `rewards` (a producer's `List<RewardSpec>`; both progression engines supply it deferred) paints one `RewardToastLines` row per readable reward on the in-page toast, tuned by `Toast.Rows` (`Show` / `Max` before the shared "+N more" line); `Args` names which of the moment's own values fill the key's blanks, filling the numbered and same-named slots alike, and a line naming a value the moment did not carry is skipped rather than shown with a gap; `Variants` (`{When, <groups>}`, first match overlays only what it restates) let ONE file word several cases of the same moment; `Toast.EveryPercent` keeps a progress moment from chattering. `FeedbackEngine.fire(momentId, Subject, args)` reads the fold and drives `Notify`/`EventTitles`/`Sound3D`/`CommandRunner`. **It knows nothing about what produced the moment** - a progression engine, a shop, a minigame round and a mod that does not exist yet all reach the same authoring surface - so joining a producer to it belongs to a layer that sees both ends, which is exactly what `zc-objectives`' `ProgressionBootstrap.registerFeedbackMoments` (called from the root's `setup()`) does for the two progression engines. **zc-presentation ships the neutral default file for each of the seven moments the progression engines announce** plus `ziggfreedcommon.feedback.lang` in nine locales, so a bare server gets feedback out of the box; a consumer's same-id file wins by pack order.
- **[`ui/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/CustomHudHelper.java)** - `CustomHudHelper` (install a consumer-built `CustomUIHud` + strip/restore the native HUD) + **[`ui/ZigRichButton`](zc-presentation/src/main/java/com/ziggfreed/common/ui/ZigRichButton.java)** (the CLICKABLE-rich-text primitive: a vanilla `Button` element wrapping a `Label #Label`, so parameterized / glue / `Msg.join` text substitutes on `.TextSpans` - what a `TextButton.Text` cannot do; `text`/`color`/`iconStart`/`iconEnd` helpers, the shared tintable `@ZigRichRowBtnStyle` + `@ZigRichPrimaryBtnStyle` + reusable `@ZigRichBtn` component in `ZigButtons.ui`) + **[`ui/UiRetint`](zc-presentation/src/main/java/com/ziggfreed/common/ui/UiRetint.java)** (the generic palette-to-selector retint primitive: `retintColor`/`retintButtonStates`/`retintStyleStates`/`swapPatch`/`isSixDigitHex` - resolved hex + selector params only, no theme model / gate / policy; `ToastRenderer`/`DialoguePage`/`LeaderboardPage`/`PartyInvitePage` converge onto it, and the MMO's `ThemeRetint` is a thin policy wrapper over it) + **[`ui/theme/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/theme/Palette.java)** (the mod-agnostic theme VALUE MODEL `Palette` + `ThemeRecord` that pairs with `UiRetint`: pure colour-slot + optional-texture data + a neutral `premium` tag, foldable over `AbstractKeyedAssetConfig`; license-free - the consumer owns the gate. Lifted from the MMO's `UIThemeConfig`), plus **[`ui/toast/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/toast/CLAUDE.md)** - the lifted transport-agnostic in-page toast engine (`ToastController`/`ToastRenderer`/`ToastSpec`/`ToastLine`/`ToastKind` + a `ToastablePage<T>` page base, shipping its own neutral `Pages/ZigToast.ui`). i18n is parameterized OUT (pre-built `Message`s only). The hyMMO MMO is the shared-file owner and re-derives its own `ToastablePage` over this engine. Plus **`ui/SettingsUiUtil`** (the domain-free settings-form binding helper: `populate`/`bindDropdown`/`bindTextField`/`bindButton`/`bindNavigation` + `setStatus`/`setToggle`/`setTabActive`, all text on `.TextSpans` via `ZigRichButton`, never a `TextButton.Text` sink), **[`ui/form/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/form/CLAUDE.md)** (the generic settings-FORM engine built on top: `FieldSpec` + `SettingsForm` render/refresh/collect appendable rows from one ordered field-spec list, plus five `Pages/ZigForm*Row.ui` templates - see its router), plus **`ui/icon/IconRenderer`** (the ONE seam that paints an `icon.IconSpec` into a row or a chip: a row ships two sibling widgets, an `ItemIcon #IcoItem` and an `AssetImage #IcoTex`, and this toggles the right one by `.Visible` with the item id winning when both are set, so no surface re-decides item-versus-texture and a row with nothing to draw keeps both hidden), and **[`ui/hud/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/hud/CLAUDE.md)** (RPG Stations extraction, phase 1 leg 1: `KeyedCustomHud`, the shared keyed in-world HUD overlay base lifted config-free from the MMO's `MmoHud` - position anchor / two push-throttle strategies / native register-lookup-broadcast over `HudManager`, with a self-contained player-lookup + guarded-logging seam so it needs no consumer utility; the MMO's `MmoHud` re-parents onto it, RpgStations' own summary HUD extends it directly - plus `HudPosition`, the lifted mod-agnostic HUD layout value: corner presets via `parse`/`isValidPreset` + `toAnchor(w,h)`/`toAnchorContentHeight(w)` pinning exactly one edge per axis; no consumer-specific defaults). The reusable `Pages/ZigListRow.ui` CRUD row (title/subtitle + optional badge + Edit/Remove) pairs with them for a list editor. Plus **[`ui/rows/`](zc-presentation/src/main/java/com/ziggfreed/common/ui/rows/CLAUDE.md)** - the fixed-slot icon + rich-text LEDGER row primitive (`SummaryRow` value + `SummaryRowRenderer` painter): a reward / results / summary LEDGER paints `SummaryRow`s into pre-declared `#Prefix0..N` slots (`#Icon` + `#Name`) with overflow, all colour/bold baked into each row's `Message` by the caller. A SIBLING to `ui/toast/ToastLine` (same icon-id + `Message` shape; a toast is transient + transport-welded, a row is a persistent fixed-slot ledger), not unified - see its router.
- **[`world/`](zc-world/src/main/java/com/ziggfreed/common/world/CLAUDE.md)** - the **world-targeting** layer (the embeddable `WorldSelector` group codec authored under the key `Where`, the `MatchRank` specificity ladder, the `WhereValidator` audit, and `WorldIdentity`'s guarded read of which worlds the server has): **a world is named by what it is CALLED or by the gameplay config it runs, and by nothing in between**, so what a file targets is readable on its own page and every rule sorts on ONE ladder (GameplayConfig exact > exact name > longest partial core > bare `*`). **`{Match, GameplayConfig, ExcludeMatch}` is the ONE spelling of "which worlds?" everywhere** - an NPC placement's `Where`, an MMO world rule's `Where`, a mob-scaling world file's `Where` and a dialogue `World` condition's `Where` are all this same group. A bare word under `Match` is an EXACT world name, so a file targeting the main world authors `["default"]`; a server whose main world is named something else overrides that file's `Where` through the asset type's own owner layer, and the validator's `MATCHES_NO_LOADED_WORLD` finding makes a misconfigured one loud instead of silent. Plus `BlockOps` (single-block read/write at world coordinates over the engine's CURRENT block surface - section lookup + `BlockSection` read + `BlockOperations.setBlock`/`setBlockInteractionState`, never the deprecated `World.getBlock`/`WorldChunk` family; an unloaded section answers null/false and is never loaded); `SurfaceProbe` (top-solid-Y column probe via `World.getBlock`, skipping transparent foliage; world-thread, try-guarded) to floor-snap runtime-placed prefabs/entities onto procedural terrain; `SpawnPlacement` (ring-around / near-player runtime spawn positions, each surface-snapped with a foliage-skip set so a runtime spawn lands on GROUND, not canopy).
- **[`cast/`](zc-cast/src/main/java/com/ziggfreed/common/cast/CLAUDE.md) (+ `cast.step/`)** - UNCHANGED this cycle: the cast RUNTIME primitives layer the new interaction-composition framework (`interaction/`) composes with (`interaction/target/` builds on `RaycastTargeting`/`BlockRaystep`), never rewritten or renamed. The reusable cast / ability RUNTIME layer: the GENERIC step-dispatch kernel (`StepHandler<C,S,R>` + `StepRegistry<K,C,S,R>` + `StepSemantics<C,S,K,R>` + `CastKernel<C,S,K,R>` - parameterized over a consumer's own context / step / result types, no baked-in step vocabulary, byte-parity with a hand-written dispatch loop) + `OnHitRegistry` (per-consumer on-hit builder dispatch, BiConsumer AND `HitAction` tables) + `HitContext` / `HitAction` / `HitResolver` (the evolvable per-hit application seam: a `ComponentAccessor` carrier so a store AND a command buffer both fit, an opaque origin-decorator + sound-sink so nothing consumer-specific enters the jar) + `ObserverRegistry<E>` (per-consumer guarded register/fire) + `CastParams` (lenient param coercion) + `ArmedStateStore` (per-caster next-hit / invulnerability state with an opaque attribution payload, caller-supplied UUID key) + `RaycastTargeting` / `BlockRaystep` (entity + block look-ray targeting) + the per-world tick partition (`WorldEvictors` JVM-global world-resolve + eviction fan-out, `WorldKeyedQueues<E>` self-evicting per-world queue partition, `AbstractWorldFrameSystem` the abstract per-world drain base, riding the engine's own once-per-world-per-tick `TickingSystem` dispatch with no dedup gate) + `ModelParticleService` (guarded `ParticleUtil` spawns). Ships EMPTY of content; a consumer supplies its own step / context / result types, registers its own on-hit + step handlers and its own concrete drain subclass. Additive-only once frozen. World-thread where it touches the store; pure/thread-safe otherwise.
- **[`instance/effect/`](zc-effects/src/main/java/com/ziggfreed/common/instance/effect/CLAUDE.md) + [`encounter/`](zc-instance/src/main/java/com/ziggfreed/common/instance/encounter/CLAUDE.md) + [`metadata/`](zc-instance/src/main/java/com/ziggfreed/common/instance/metadata/CLAUDE.md)** - the reusable ENCOUNTER framework (see the blockquote above): `EntityEffectService`/`EffectBand`/`EffectBandLadder` (timed + banded per-entity effects over `EffectControllerComponent`), `SpawnRoster<T>`/`SpawnUnit<T>`/`EncounterDirector` (weighted tier-gated spawn picks + cooldown/cap/per-round-max wave gating, pure logic), `RoundMetadata` (outbound integration envelope). Kweebec 0.5.0 is the exemplar; the MMO's dungeons/raids are the target.
- **[`instance/zone/`](zc-instance/src/main/java/com/ziggfreed/common/instance/zone/CLAUDE.md)** - `ZoneHoldTimer`: a continuous "the required group must hold this zone TOGETHER for X seconds, RESET on break" objective timer (pure logic, `nowMillis` injected like `EncounterDirector`). `update(occupants, required, now)` latches complete; `isHolding()`/`remainingSeconds(now)`/`progress(now)` drive a HUD. The consumer owns WHO/WHERE/what-on-complete. Exemplar: Kweebec's Chase extraction platform.
- **[`encounter/`](zc-encounter/CLAUDE.md)** - the boss framework over Update 6's native `EncounterManager` (module `zc-encounter`). The fight (states, phases, health thresholds, in-place role swaps, adds, bar, music, membership, hot reload) is a native script under `Server/EncounterManager/` and the engine runs it; the library adds only what native cannot do, all outside the script: `EncounterSignalSystem` turns the script's `SignalWorldEvent` beats (`zc:engaged`, `zc:phase:<State>`, `zc:wave[:<label>]`, `zc:defeated`, `zc:reset`) into six native `IEvent`s through one `NativeEventSeam`, `EncounterDeathSystem` latches the precise defeat, `EncounterDamageSystem` (INSPECT group, observe-only) and the tick feed a `(runId, playerUuid)` `ParticipationLedger` whose shares ride the terminal events, `EncounterScaling` keys one `HealthUtil` modifier (`zc_encounter_scale`) reconciled after every phase, and `EncounterLoot` / `EncounterFeedback` / `EncounterDiscovery` pay what the `EncounterBindingAsset` row owes. Five registered encounter Types (`ZigGrant`, `ZigFeedback`, `ZigScaleTarget`, `ZigMembers`, `ZigFactor`) let a pack-only boss pay, notify and scale with zero Java. Four seams the root fills (`EncounterSeams`: attribution, power, subject, reward queue), each reporting once when consulted unfilled. `EncounterEdgeTest` bans every domain-engine import. `/zigencounter list|inspect|spawn|end|state|validate|reload`.
- **[`world/placed/`](zc-world/src/main/java/com/ziggfreed/common/world/placed/CLAUDE.md)** - `PlacedBlockLedger`, the ONE answer to "did the player put that block or item there themselves?", plus `PlacedBlockRecorder`, the single ECS system that writes it. Any mod counting block breaks or pickups wants this, so it is the library's rather than a consumer's: the objectives producers, a consumer's XP path and a statistics counter all read the same rows and can never disagree. A placed BLOCK is one bit on its own chunk section (`PlacedBlockSection`, a plugin-registered `Component<ChunkStore>` laid out like the engine's own `BlockPhysics`), so it is saved and loaded with the chunk, only loaded chunks cost anything, and there is no file to scan, snapshot or rewrite; placed ITEMS stay an in-memory count that expires in minutes. `Policy` is three independent knobs read live (enabled / which placers are guarded / item expiry), so a consumer with its own owner config installs one and never re-pushes.
- **[`world/pattern/`](zc-world/src/main/java/com/ziggfreed/common/world/pattern/BlockPattern.java)** - generic structure-pattern matching over a caller seam: `BlockPattern<P>.compile` (ordered cells, each an anchor-relative integer offset plus an OPAQUE caller payload; exactly one anchor cell, re-based onto the origin) expands up to 8 precomputed `PatternVariant`s (4 yaw quarter-turns x optional X-mirror; one positive quarter-turn maps `(x, y, z)` to `(z, y, -x)`, the engine's own yaw `Ninety` turn, so `yawQuarterTurns` lines up with the engine `Rotation` ordinals). Matching walks the cells through the `BlockReader` (null = cannot tell = fail; an unloaded section never matches, never loads) and `CellPredicate<P>` seams, short-circuiting on the first fail; `anchorFromCell`/`matchFromCell` derive the implied anchor from any known cell position and answer a `PatternMatch`. `PatternIndex<P>` maps exact block item ids to caller-registered (pattern, variant, cell) candidates plus `maxBoundingRadius` for proximity pruning; zc holds ZERO matching vocabulary (what a cell accepts is the payload's meaning to the caller). `BlockReader.over(chunkStore)` is the one live wiring. See `world/CLAUDE.md`.
- **[`world/record/`](zc-world/src/main/java/com/ziggfreed/common/world/record/BlockRecordSection.java) + [`world/stash/`](zc-world/src/main/java/com/ziggfreed/common/world/stash/BlockStashes.java)** - the per-block RECORD substrate and its first consumer. `BlockRecordSection<T>` is the generic per-block keyed-record `Component<ChunkStore>` over a caller-supplied payload codec: registered once per registry id at setup, saved and loaded with the chunk, an empty section costing the bare version marker; the typed `Handle` carries the position-facing accessors, its mutators flag the owning `ChunkSection` for a save themselves (the engine watches no plugin component), a caller mutating a fetched record in place owes one `markDirty`, and `clone` deep-copies through the payload codec so the world thread can never tear an in-flight IO-thread save. `world/stash/` rides it as the ONE "what is this block holding for players?" store (`BlockStashes`, registry id `ZigBlockStash`): `BlockStash` piles + world-GAME-TIME clocks (an outage advances them by zero) + an opaque tag, `StashPile` insertion-ordered counted items + one metadata-intact `ItemStack` + pending cycle counts - pure storage, every accept/take/share rule staying with the consumer. Both world-thread only; see `world/CLAUDE.md`.
- **[`worldmap/`](zc-world/src/main/java/com/ziggfreed/common/worldmap/CLAUDE.md)** - `WorldMapMarkers`: place/remove world-map + compass POI markers over the native `WorldMapManager` - global (all players, via the engine `POIMarkerProvider`) AND per-player (`registerProvider` returning each player's own markers). Build a marker with `marker(id, icon, x,y,z, name)`. Needs the world's compass/map enabled to render. `MapDiscovery` (+ `DiscoveryMode`) builds on it: a hidden-until-discovered POI tracker with two orthogonal axes - trigger (`OFF`/`ON_INTERACT` via `discover` / `PROXIMITY` via the pure `revealWithin`) x per-POI visibility (`PER_PLAYER`/`SHARED`); `attach(world)` enables the compass + registers one provider.
- **[`dialogue/`](zc-dialogue/src/main/java/com/ziggfreed/common/dialogue/CLAUDE.md)** - the generic branching NPC dialogue engine: ONE process-wide `DialogueEngine` every mod registers into ADDITIVELY, over the process-wide `DialogueTypeTable` that reads the files (one store, one read, so the decode vocabulary and the behaviour behind it are both everybody's, and an authored option may mix two mods' actions). It seeds the generic `Goto`/`Close`/`Remember`/`Forget`/`MarkTalked`/`OpenPage` actions, the `Remembered`/`NotRemembered`/`World`/`Factor` + `AllOf`/`AnyOf`/`Not` conditions over the authored `Memories` declarations, and the QUEST-aware vocabulary (`QuestState`/`ReadyToTurnIn`/`HasReadyToTurnIn` + `Accept`/`TurnIn`) reading through zc-progression's narrow `QuestStateReader` seam plus a consumer answer set; `Once` is the keyless seen-ness knob on a `Start` entry or an option. Option shorthand is a structured schema leaf folded post-decode, not a pre-parse rewrite. Plus the executor, native-`Parent` asset inheritance (`InheritMapCodec`, no resolver of ours) and a `Finding`-reporting content audit, and a `page/DialoguePage` whose `.ui` ships in this jar. The hyMMO MMO consumes + extends it; a minigame contributes its own two or three types and takes the generics for free. NOT a static-util like the others - a consumer registers into it at `setup()`, and `DialogueEngine.builder()` remains for an isolated test engine.
- **[`codec/`](zc-core/src/main/java/com/ziggfreed/common/codec/CLAUDE.md)** - inheritance-aware codec + raw-JSON merge primitives: `InheritMapCodec` (per-key map merge under native `Parent` inheritance) + `JsonParentResolver` (generic parent-chain resolution over a keyed raw-JSON pool, for asset families merging across store + owner-dir layers) + `JsonTreeCodec` (verbatim capture of an authored JSON subtree, numbers keeping their authored spelling so an integer survives a second decode) + `DeferredCodec` (the stand-in for a codec that cannot be referenced at class-load time, resolved fresh at each decode/encode and forwarding `InheritCodec`: for a codec built from a startup-registered vocabulary - the dialogue conversation fields - or an engine constant like `ItemStack.CODEC` whose class-init must not ride along - the stash `Unique` leaf) + the shared authorable leaf types `Vec3`/`Vec3i` (doubles for a spatial shift, integers for a block-cell offset that must refuse a fractional value)/`Rotation`/`TagMatch`.
- **[`counter/`](zc-core/src/main/java/com/ziggfreed/common/counter/CLAUDE.md)** - named long TALLIES per subject: `CounterMap` (an owner-free bag with `add` / `highWater` / `mergeSums` / `mergeHighWater`), the `CounterStore` persistence seam plus a ready-made in-memory one, and `Counters` (the engine, with CATEGORY keys so one flat store carries a grand total beside any number of per-thing breakdowns). Zero engine types, zero domain vocabulary. **`counter/` and `stats/` never merge** - a "how many times has this subject done X" number is a tally, a "what does this sword add to Attack Damage" number is an item-carried stat; the rule is stated in full in `stats/CLAUDE.md`. First consumer: the instance leaderboard's lifetime view, whose `LeaderboardEntry` holds a `CounterMap` so the record path and the cross-bucket aggregate share ONE summing authority.
- **[`subject/`](zc-core/src/main/java/com/ziggfreed/common/subject/Subject.java)** - `Subject(UUID id, String name, @Nullable Object handle)` plus `of` and `handleAs`: WHO an engine operation is about, and the ONE identity vocabulary every engine in the library speaks. The handle is opaque - a consumer stashes an engine player reference, an entity handle or a session object and gets it back typed - so no engine ever learns a particular player representation, and a gate or reward handler written against one engine reads naturally when another calls it.
- **[`progress/`](zc-progression/src/main/java/com/ziggfreed/common/progress/CLAUDE.md)** - the shared PROGRESSION cores: `ObjectiveDef` (one authored objective: kind, target, match mode, qualifier, amount, zone, order, hand-in lock), the open `ObjectiveKind`/`ObjectiveKindRegistry` vocabulary (23 engine-generic kinds pre-seeded, per consumer), the matching core `MatchMode`/`ObjectiveMatch` (ONE forgiving rule: case-insensitive targets, empty target = match-all, empty qualifier = only-unqualified) + `ZoneRef` + `ZoneLocator` (the one read of where a player is, off the engine's `WorldMapTracker`), `ObjectiveProgressState` (how far along, plus its byte-stable `"current/required"` wire form), `ObjectiveIndex` (the inverted kind -> objectives index the dispatch hot path walks, owner-agnostic via accessors) and `DispatchOptions` (two independent switches, three named combinations), plus `ObjectiveComposer`/`NeutralObjectiveComposer` (how a key-less step reads as a sentence: the consumer-installable slot over the shipped nine-locale per-kind family, stamped per step by both folds). TWO peer lifecycle engines rest on these, quest and achievement; nothing here may name either. Beside them sit `progress/asset/` (the authoring GROUPS both engines share - `ContentTextAsset`, `ObjectiveLeafAsset`, `RewardEntryAsset`, `ProgressEditorDataSets` - declared once so their field names cannot drift) and `progress/gate/` (`GateClause`/`GateSpec`/`GateKind`/`GateKindRegistry`/`GateEvaluator`/`GateRefusal`, the ONE requirement model behind every `Requires` block - `allRefusals` reports every unmet requirement as a structured record and the token API is its byte-for-byte projection), plus `ProgressDispatchTap`, the side-channel both engines' dispatches feed. Its `runtime/` holds THE shared runtime and every contribution a consumer registers into it, incl. (2026-08) `MomentListener` + `Moment` + the open `MomentPayload` marker (a REACTION to every produced moment, fanned FIRST from `ProgressDispatch.fire`, before the subject test and both system gates - not the tap, not a gate) and `KillAttribution` (a non-player attacker acting for a player, first real answer wins) + `KillQualifier` (a qualifier the killed entity carries - e.g. a companion mod's difficulty tier - asked once at fire time and stamped into the one `KILL_ENTITY` dispatch, first real answer wins).
- **[`quest/`](zc-progression/src/main/java/com/ziggfreed/common/quest/CLAUDE.md) (+ [`quest/asset/`](zc-progression/src/main/java/com/ziggfreed/common/quest/asset/CLAUDE.md), `quest/event/`)** - the consumer-agnostic QUEST lifecycle over those cores: a per-consumer `QuestEngine` owning matching, ordering, cooldowns, hand-ins, completion and payout bookkeeping, with the consumer supplying storage (`QuestProgressStore`, plus a ready-made in-memory one), gates, inventory access and naming. Ships ZERO content and ZERO domain vocabulary, enforced by an agnosticism test that walks the module source. What a REWARD is lives one module down in [`loot/reward/`](zc-loot/src/main/java/com/ziggfreed/common/loot/reward/CLAUDE.md) (shared with every other payout site), and a surface that only READS a player's quests - a conversation above all - takes the narrow `QuestStateReader` seam rather than the engine. `quest/asset/` is the authoring half (the `Server/ZiggfreedCommon/Quests/<id>.json` schema with native `Parent` inheritance + a per-objective-id merge, the generator, the `Requires` gate vocabulary, the pool + validator); `quest/event/` is six native `IEvent<Void>` POJOs (accept, objective progressed, completed, claimed, abandoned, tracked-pin change).
- **[`achievement/`](zc-progression/src/main/java/com/ziggfreed/common/achievement/CLAUDE.md) (+ [`achievement/asset/`](zc-progression/src/main/java/com/ziggfreed/common/achievement/asset/CLAUDE.md), `achievement/event/`)** - the ALWAYS-ON peer lifecycle over the same cores: nothing is accepted, nothing is abandoned, nothing comes back on a cooldown, so every criterion of every catalogued achievement listens from the first event and one earns itself the moment its criteria are met. `AchievementEngine` owns matching, counting, earning, collecting, points, milestones and pins; the consumer supplies storage (`AchievementProgressStore`, plus an in-memory one) and `AchievementGates` (`canProgress`/`canUnlock`/`canReceiveRewards`/`visible`, all default-yes). **Criterion progress is keyed by the criterion's ID** (`"<id>#<critKey>"`, the authored `Criteria` map key, merged per key under `Parent` exactly like a quest's `Objectives`), so renaming a key starts that criterion over while re-authoring never silently re-points anyone's tally. One `Rewards` group, two buckets: `Auto` lands on earning, `Claim` waits to be collected. `achievement/asset/` is the authoring half (`Server/ZiggfreedCommon/Achievements/<id>.json`, Pattern A + native `Parent`, sharing the text / objective-leaf / reward-entry / gate groups with quests); `achievement/event/` is three native `IEvent<Void>` POJOs.
- **[`objectives/`](zc-objectives/src/main/java/com/ziggfreed/common/objectives/CLAUDE.md)** - what a bare server gets from the SHARED progression runtime: `ProgressionDefaults` (the default per-player component store plus the five generic native-event producers - block break / mob kill / craft / pickup / place block - each carrying a typed payload to every registered `MomentListener` reaction) and an in-game two-tab objective book opened from an ordinary item, plus the tracked-quest HUD (`objectives/hud/`: a `KeyedCustomHud` on every player, repainting on the quest engine's six native events through a per-tick coalescer, NO tick; a consumer themes / gates / positions it through `TrackedQuestHudDeps`). **There is no second runtime here.** These are contributions like any other: they are registered into `progress.runtime.ProgressionRuntime` from this module's setup, and a consumer mod that runs its own progression REPLACES the parts it answers for (its own store, its own gates) through the same registration surface. **Wanting your own PERSISTENCE is NOT a reason to take the store slot**: a consumer with a database or a write-behind cache registers `ProgressionDefaults.onProgressDirty` / `onProgressFlush` and keeps the default store as THE store, since two stores would be two versions of one player's state. The five PRODUCERS are not among them: they always run, nothing claims a kind and nothing stands down, because a consumer never registers a competing producer for a native event the library already covers. A mod with a NET NEW moment calls `ProgressDispatch.fire` from its own event system, with no registration at all. The book reads the one runtime, so it shows whatever any mod on the server contributed. **`objectives.command` is `/zigprogress`**, the admin family over that same runtime (`quest` / `achievement` / `memory` groups; engine-derived permission nodes; its own nine-locale `ziggfreedcommon.progression.admin.lang`; `ProgressAdminKeysTest` fails the build on a verb with no help line), copied rule for rule from `commerce.command`; a consumer mod wanting its own spelling registers an alias over the same engines. See its router and [`objectives/command/CLAUDE.md`](zc-objectives/src/main/java/com/ziggfreed/common/objectives/command/CLAUDE.md). **`objectives.flair` is the flair GRANT surface**: the `Flair` reward kind (unprefixed, `Flair`/`FlairId` param, a no-op for a flair already held, a `zigflair grant` retry line when there is no live player), the `FlairUnlocks` write facade every grant goes through (fires `ZigFlairChangedEvent` and the authored `Flair_Unlocked` moment on a real change only), `FlairChipReading` (`flair.<id>.name` under any loaded namespace, else the id spelled out) and the `/zigflair grant|revoke|list` family (`ziggfreedcommon.flair.lang` + `ziggfreedcommon.flair.admin.lang`, 9 locales; `FlairAdminKeysTest`), all registered by `FlairBootstrap` from the wiring root's ordered list.
- **[`validation/`](zc-core/src/main/java/com/ziggfreed/common/validation/CLAUDE.md)** - the ONE audit-finding vocabulary: `Finding(severity, code, message, sourceId, domain)` + `Severity{ERROR,WARNING,INFO}` + `ValidationReport` (counts, one-line formatting, `logAll` to a `Consumer<String>` split by tier). Every content validator in the library reports through it - `WhereValidator`, `NpcPlacementValidator`, `DerivedFactorValidator`, `DialogueStructureValidator`, `LootableValidator`, `QuestPoolValidator` + the quest store, `AchievementPoolValidator` - so a consumer folds several domains into one report without learning a shape per validator. **The load-bearing rule: an unknown id is a WARNING, never an ERROR** (its owner may register later, or be a mod the author expects some servers not to install). Every entry point returns `List<Finding>`; a consumer with its own finding type adopts these rather than asking for a second shape here.
- **[`command/`](zc-core/src/main/java/com/ziggfreed/common/command/CLAUDE.md)** - `CommandRunner`, the authored-command primitive: map-driven `{key}` substitution (the vocabulary belongs to the consumer, nothing baked in), the `/give` positional-quantity fix (the engine reads a count ONLY from `--quantity=N`, so a positional one silently delivers a single item), and a per-call guard reporting to a `Consumer<String>`. Runs AS THE SERVER CONSOLE over `util/CommandExecutor`, behind a one-method `Dispatcher` seam so it is fully unit-testable.
- **[`asset/`](zc-core/src/main/java/com/ziggfreed/common/asset/CLAUDE.md)** - the generic Pattern-A registrar lift + the framework asset-store plumbing (`FrameworkAssetRegistrar`, `AbstractKeyedAssetConfig`, `AbstractRawJsonAsset`, `AssetStoreRegistrar`/`AssetMergeAdapter`); Common OWNS these stores, a consumer authors JSON + reads the resolved config back.
- **[`entity/`](zc-entity/src/main/java/com/ziggfreed/common/entity/CLAUDE.md)** - `PlayerModelService` (swap a player's visible model); `PlayerPuppetService` (the "mount + hide the player, show a puppet performing the work" mechanism - clone a live `PlayerSkin` onto a networked spawned entity with held-item mirroring and a render-guaranteed animation surface, plus the paired `Scale` self-hide/reveal primitive, lifted from a consumer's in-game-proven spike harness); `ItemPropEntityService` (spawn/despawn a networked item-prop entity - a placed-input display, a shop pedestal - lifted verbatim from a consumer's own display mechanism, itself copied from the engine's sanctioned Entity Spawn Page exemplar); `PuppetNav` (a bounded A* grid-walker over `CollisionModule` for pathing a bare-`Holder` puppet between nearby stand-spots, without the full NPC Role/steering framework).
- **[`entity/performer/`](zc-entity/src/main/java/com/ziggfreed/common/entity/performer/CLAUDE.md)** - the station-performer seam: one `StationPerformer` contract (`spawn`/`despawn`/`presentAt`/`walkTo`/`setProp`/`playClip`/`isAlive`/`ref`) so a station-puppet controller presents a "double performing the work" without branching on the look source, every mutating method taking a FRESH per-call `ComponentAccessor` the caller threads from its own current frame. Two backends: `HolderPerformer` (the bare-`Holder` skinned puppet, driving `PlayerPuppetService` + `PuppetNav`) and `NpcRolePerformer` (a Role-driven `NPCEntity` using native gait + engine A*, prop/clip best-effort pending in-game smoke). `WalkHandle`/`FailedWalkHandle` is the poll-driven walk handle; `PerformerLook` is the orthogonal-knob appearance/behaviour config; `PerformerSpawnCtx` is the one-shot spawn descriptor (accessor-flex for an engage-time `CommandBuffer` vs a boot/command-time live `Store`); `PerformerIdentityComponent` (a registered ECS component the library's own `EntityBootstrap` registers once at `setup()`, not a consumer) + `PerformerReconciler` (orphan-reconcile sweep, pure `KEEP`/`DESPAWN`/`REBIND` policy factories) round out persistence + cleanup. World-thread only, fully try-guarded; pure decision cores are unit-tested, the engine-touching paths await maintainer in-game smoke.
- **[`stats/`](zc-entity/src/main/java/com/ziggfreed/common/stats/CLAUDE.md)** - a unified per-stack + tag entity-stats bridge so item-carried stats become native `EntityStatMap` modifiers instead of a per-mod at-use metadata fold. `StackStats` (the FILE lives in zc-core under this split package) is the ONE generic per-stack stat/enhancement record (metadata key `"ZigStackStats"`; percent channels in whole percent points, flat channels raw; `read`/`merge`/`stampReplacing`/`stampReplacingWithCount`). `EquipStatBridge` is the equip-watcher engine converting held/armor/offhand `StackStats` (plus a held item's native `Utility.StatModifiers`, which the engine never applies itself) into keyed native modifiers via diff-skip + stale-key sweep, over three abstract ECS trigger bases a consumer subclasses once each (class-keyed registry); `bridgedSum` returns the bridge's own current additive contribution to a stat for a DOT branch to subtract. `StatMirror` is the idempotent keyed put/remove of a single native modifier (mirroring a derived scalar with zero at-use lookups); `StatChannelAudit` is a boot-time channel-presence check (one `SEVERE` line per missing stat id, the load-order silent-drop guard). All world-thread, try-guarded, config-free.
- **[`interaction/`](zc-cast/src/main/java/com/ziggfreed/common/interaction/CLAUDE.md)** - common now owns the GENERIC interaction-composition framework: custom-Type registration plumbing (`interaction/type/`), the fire-time parameter-fold seam (`interaction/param/`), the server-authoritative targeting engine (`interaction/target/`), and chain fire, an input-readiness probe, plus a cycle-guarded chain walk (this package) - a consumer keeps only policy. `NativeChainFire.fire(store, casterRef, interactionId, interactionType[, decorator])`: fire a named engine `RootInteraction` chain by id via `InteractionManager.initChain`+`queueExecuteChain` (the proven server-side chain-trigger mechanism; ANY registered id, vanilla/pack/plugin, is fireable - one global unscoped asset store), now with an ADDITIVE 5-arg overload taking a `Consumer<InteractionContext>` decorator invoked after `initChain` and before `queueExecuteChain` - the per-fire context door a consumer pairs with `interaction.param.CastScopes.decorator` to attach a `CastScope` that survives into every Selector hit fork (a throwing decorator is caught, logged once, and the chain still fires; the existing 4-arg form is unchanged, delegating with a null decorator). Fail-closed throughout (missing component / unresolved id / engine throw all degrade to `false`, one guarded warn each, never a throw). Plus `NativeInputGate.probe(store, ref, heldItem, heldStack, rootInteractionId)`: can this native root actually DO anything for this entity right now, read off its own opening conditions without running it, for a consumer putting its interaction on a key an item already uses (`satisfied` / `hasResourceGate` / `readyToSpend`, every uncertainty resolving to not-ready). Plus `ChainWalker`/`ChainWalk`/`ChainNode`: transitive, CYCLE-GUARDED, fail-soft resolution of a chain into its reachable nodes (for a validator or a description renderer) over the engine's own chain-walk plumbing with a guarding collector - the engine ships no cycle/depth guard anywhere, so a self-referencing chain stack-overflows the server at load without one; reports depth/node-count caps and detected cycles rather than truncating silently. The composition-by-reference primitive for a consumer authoring content that fires native interaction chains (station presentations/moments, dialogue actions, ability bodies) instead of re-deriving engine motion/VFX. World-thread only.
- **[`interaction/type/`](zc-cast/src/main/java/com/ziggfreed/common/interaction/type/CLAUDE.md)** - the custom interaction-Type registration toolkit: `InteractionTypeSpec` (a Type's name/class/codec held behind a `Supplier` so a spec is constructible without forcing the Type's codec class-init - the RangeValidator/HytaleLogger clinit trap), `InteractionTypes.register`/`registerAll` (fail-soft registration over the engine's own codec registry, called once from a consumer's `setup()`), `InteractionCtx` (null-safe context accessors: firing entity, chain owner vs Selector-forked target, held item, position/eye position/look direction, world, player, entity id), and `InteractionBody`/`InteractionOutcome` (the two documented chain-state conventions a Type's `firstRun` resolves through - a silent Finished SKIP for a gate miss vs a Failed resolve for a hard error or a deliberate branch that suppresses the chained native step - plus a `guard` wrapper turning any body throw into a logged Failed). World-thread.
- **[`interaction/param/`](zc-cast/src/main/java/com/ziggfreed/common/interaction/param/CLAUDE.md)** - the fire-time parameter-fold seam: `CastScope` (an immutable, field-additive per-fire payload - scope id, caster id, an opaque consumer payload, a string var overlay) + `CastScopes` (stashes/reads a scope on a live interaction context's meta store, surviving every Selector hit fork via the engine's own context-duplicate copy) + the `ParamFoldRequest`/`ParamFoldResolver`/`ParamFold` SPI + `ParamSlot` (an authorable `{Key, Base}` codec leaf that folds its authored base through a per-consumer resolver at fire time, degrading to the authored base on no resolver / a blank key / a throwing or non-finite resolver). Common only asks the question and guards the answer; a consumer supplies its own fold. World-thread.
- **[`interaction/target/`](zc-cast/src/main/java/com/ziggfreed/common/interaction/target/CLAUDE.md)** - the server-authoritative targeting engine, the trustworthy counterpart to the engine's own Client-trusted selector sweep: `TargetQuery` (an immutable, orthogonal-knob sweep description - distance range, optional cone angle, optional vertical extent, broad-phase inflate, line-of-sight requirement, a deterministic cap, exclusions, a consumer filter) + `TargetHit` + `TargetSweep` (`volume`/`ray` narrow phases over `cast.RaycastTargeting`/`cast.BlockRaystep`, one shared post-filter pipeline) + `LineOfSight` (a block-grid test over `cast.BlockRaystep`). Deterministic by design (sorts + caps instead of the engine's random sample), so a targeting result is authorable and testable. World-thread.
- **[`effect/`](zc-effects/src/main/java/com/ziggfreed/common/effect/CLAUDE.md)** - `NativeEffectUtil`: apply/remove a native `EntityEffect` asset by id over `EffectControllerComponent.addEffect` - the ASSET-AUTHORITATIVE 3-arg overload (`apply`, duration/overlap/infinite all from the asset) and the DURATION-OVERRIDE 5-arg overload (`applyFor`, caller picks duration + `OverlapBehavior`, reusing one asset's other fields at a different lifetime), plus `remove` (id -> current engine index -> `removeEffect`). Fail-closed throughout. `AppliedEffectTracker` is the companion session-scoped bookkeeping primitive: `track(ref, effectId)` after a successful apply, `removeAll(store)` strips every tracked entry and clears unconditionally - the "guarantee every effect a session applied is gone at stop()" shape. A SIBLING to `instance/effect/EntityEffectService` (that package is the Kweebec-style encounter/pace-band framework primitive with its own `applyTimed`/`applyBand`; this package is the plain id-in/id-out apply-remove-track primitive for composing native effect content by reference, e.g. a station `Presentation.Effect`/`Grants.Effects[]`) - both are legitimate, pick by shape needed.
- **[`lobby/`](zc-instance/src/main/java/com/ziggfreed/common/lobby/CLAUDE.md)** - the matchmaking lobby: `MatchmakingQueue`/`LobbyService`/`RoundLauncher` (queue keys, group/solo join, launch-timer countdown), locale-free `QueueMessages`.
- **[`npc/`](zc-dialogue/src/main/java/com/ziggfreed/common/npc/CLAUDE.md)** - the generic NPC `ActionOpenDialogue` (open a `DialoguePage` on press-F via a registered NPC Action) + `NpcSpawnService`/auto-spawn/placement.
- **[`party/`](zc-instance/src/main/java/com/ziggfreed/common/party/CLAUDE.md)** - the searchable-invite party: `Party`/`PartyService` + `PartyInvitePage` (Queue button carries a chosen preset, owner public/private toggle), `PartySettingsAsset`.
- **[`instance/match/`](zc-instance/src/main/java/com/ziggfreed/common/instance/match/CLAUDE.md) + [`arena/`](zc-instance/src/main/java/com/ziggfreed/common/instance/arena/CLAUDE.md) + result/ + preset/ + [`reward/`](zc-loot/src/main/java/com/ziggfreed/common/instance/reward/CLAUDE.md) + [`leaderboard/`](zc-instance/src/main/java/com/ziggfreed/common/instance/leaderboard/CLAUDE.md) + [`play/`](zc-instance/src/main/java/com/ziggfreed/common/instance/play/CLAUDE.md)** - the instance-experience layer (see the blockquote above): match rules/verdict, `ArenaDefinitionAsset`, `MatchResult`+`ResultsPage`, `InstancePresetAsset`, `InstanceReward`+`DeferredRewards`, `Leaderboard`+`LeaderboardPage`, `PlayModePage`. `reward/` also carries `NativeLootService` (rolls a native `ItemDropList` + spawns the stacks on the ground, the XP-agnostic engine-touching half of the loot primitive). A consumer contributes its own reward-spec KIND token (e.g. `xp`) by registering a `RewardAuthoring` adapter on the shared `RewardKinds.shared()` vocabulary rather than a table of the instance layer's own; an unregistered token drops rather than granting a phantom reward. Score and win/loss eligibility are `FactorCondition`s over `loot/LootFactors`, walked by the shared gate; a preset's `RewardTableId` names a `Lootable`, whose `Pool` group is the score-tiered draw.
- **[`loot/`](zc-loot/src/main/java/com/ziggfreed/common/loot/CLAUDE.md) (+ [`loot/stamp/`](zc-loot/src/main/java/com/ziggfreed/common/loot/stamp/CLAUDE.md))** - the ONE loot core, so a station's rare find, a chest, a mob's bonus drop and a quest reward that rolls a table all behave identically on identical JSON. A `Roll` is `{Trigger, Conditions, Chance, Ladder, Grants, Cue}` read in that order; `RollEvaluator` is the PURE decision and `LootEngine` the half that acts, entirely through seams (item sink, drop-list sink, command dispatcher, reward registry), so a caller supplying none gets a full evaluation with no effects. `LootableAsset` (`Server/ZiggfreedCommon/Lootables/`) and `LootRef` are the authoring surface. `loot/stamp/` is the same shape for stats-on-an-item: `StatRollEntry`/`RollPoolAsset`/`StampSpec` authored, `StampCapEngine` the pure decision (lowest budget binds, per-stat ceiling on top, a fully-capped attempt DENIED so nothing is charged), and the write behind the pluggable `Stamper` contract with `StackStatsStamper` as the default the wiring root installs. `LootFactors` turns an instance run's score and outcome into ordinary factor readings.
- **[`loot/reward/`](zc-loot/src/main/java/com/ziggfreed/common/loot/reward/CLAUDE.md)** - the ONE reward VOCABULARY every payout site in the library shares: `RewardSpec` (a kind plus a string parameter bag, deliberately not a fixed field set), `RewardHandler` (grant now, or offer a replayable command), `RewardKindRegistry` (the open kind table, nothing pre-seeded, carrying BOTH a kind's runtime handler and its optional `RewardAuthoring` adapter for terse formats) and `RewardGrants` (the per-reward-isolated payout pass returning `granted`/`queued`/`failed`). `LootRewardKinds` ships the four the framework itself pays out (`Item`, `Lootable`, `Stamped_Item`, `Command`); the wiring root adds `Effect` from above, because the loot layer may never see the effect module (zc-objectives adds `Flair` the same way, from `objectives/flair/`), and installs the DEFAULT overflow policy (`FeetDropOverflow`: an item reward that does not fit the bag drops on the ground at the player's feet through `NativeLootService`'s tick-safe spawn seam; a consumer's own `LootRewardKinds.overflow(...)` call replaces it, null clears it back to fail-and-park). It sits in zc-loot so a quest turn-in, an end-of-round payout and an achievement unlock all pay out through the same registered kinds; a consumer registers each kind once and authors it everywhere.
- **[`i18n/`](zc-core/src/main/java/com/ziggfreed/common/i18n/CLAUDE.md)** - `Msg`, a mod-agnostic client-resolved `Message` factory lifted from the MMO's own `i18n.Msg` (RPG Stations extraction, phase 1 leg 1): `tr(prefix, key, args...)` takes the caller's OWN namespace prefix (no fixed namespace baked in, so several consumer mods share it), plus `key`/`raw`/`trNamed`/`keyNamed`/`join`/`cat` (the param-safe composite, via the shipped locale-neutral `ziggfreedcommon.fmt.cat` glue key - the filename-derived prefix is load-bearing) and `bold`/`color` fluent wrappers. The MMO's own `i18n.Msg` is untouched; RpgStations is the first consumer. `i18n/GeneratedLangPack` is the sibling primitive for a RUNTIME-generated localization overlay: writes per-locale `.lang` files from live server state and registers them as an `AssetPack` so `I18nModule` loads the keys, for a key a plugin only knows at boot (a dynamically-registered stat channel, a data-driven catalog entry).

## Conventions

`@Nonnull`/`@Nullable` on params; **[`CommonLog.LOGGER`](zc-core/src/main/java/com/ziggfreed/common/CommonLog.java) for logging** (guard the raw LOGGER behind a try/catch on any path a unit test could reach - the flogger LOGGER throws in a log-manager-less unit JVM; `util/SafeLog` does that for you). Never log through `ZiggfreedCommonPlugin.LOGGER` from library code: it is the same handle, but reaching for the plugin entry point drags the whole wiring root into a package's dependency closure. `ConcurrentHashMap` for shared maps. Every engine-touching call is try-guarded so a missing asset / bad ref degrades to a no-op, never a throw into the caller. World-thread discipline: off-thread work hops via `world.execute` before any Store/Ref/Sound/HUD/camera read; packet writes (`writeNoCache`/`write`) are thread-safe off-thread. **No em-dashes anywhere.** Package root `com.ziggfreed.common`. **UI RICH TEXT (any primitive that shows text): colour/bold/italic = native `<color is="#hex">`/`<b>`/`<i>`/`\n` markup in the `.lang`, rendered on a Label's `TextSpans` (NEVER `.Text`, which prints the tags literally); per-locale, client-parsed; `TextButton`s + String sinks can't (NOR do they substitute `{0}/{1}` params - a parameterized `Msg.tr` prints the literal `"{0}{1}"` on a `TextButton.Text`). For CLICKABLE rich / parameterized text use [`ui/ZigRichButton`](zc-presentation/src/main/java/com/ziggfreed/common/ui/ZigRichButton.java): the vanilla `Button` ELEMENT (a `ButtonStyle` container) wrapping a child `Label #Label` set via `.TextSpans`, styled by `ZigButtons.ui` `@ZigRichRowBtnStyle` (theme-tintable). The `dialogue/` page does the Label case (`#NodeText.TextSpans`). Crucial UX infra, authority: `../../.claude/research/hytale-rich-text-textspans.md`.**
- **HARD RULE - every LABELED clickable button is a `ZigRichButton` (a `Button` + inner `#Label`), NEVER a `TextButton` whose `.Text` you set from Java.** We HAVE the rich-button primitive - use it. A `Button` + `#Label` pushed via `ZigRichButton.text` (`.TextSpans`) substitutes params + renders markup + resolves cleanly; a `TextButton.Text` sink is the wrong tool (it is the exact anti-pattern this primitive exists to replace). This applies to page tabs, save / confirm / cancel buttons, list-row action buttons - anything with a label. `SettingsUiUtil.setToggle` / `setTabActive` already drive the rich `#Label`; a page authors its buttons as `Button { Style: $Z.@ZigRichRowBtnStyle; Label #Label {...} }` (or `$Z.@ZigRichBtn`) and sets each label with `ZigRichButton.text`. Do NOT reach for a `TextButton` label just because it is a single key.
- **HARD RULE - every `ItemGrid` carries a `Style`, and it comes from the shared ladder.** An `ItemGrid` declaring no `Style:` renders NOTHING: the slot is laid out and reserves its width, so the row indents as though an icon were there, and the picture never appears. It fails silently and it fails in game only, since `.ui` files are never compiled or validated. `zc-presentation`'s [`Common/ZigButtons.ui`](zc-presentation/src/main/resources/Common/UI/Custom/Common/ZigButtons.ui) holds `@ZigIconGrid18` / `20` / `22` / `24`; a page imports that file and picks the rung matching its own grid's `Anchor` width and height (`Style: $Z.@ZigIconGrid20;`). Never declare a private `ItemGridStyle` in a page: the sizes are the only thing that varies, the ladder already covers them, and a fifth size is one more rung here rather than an eighth copy of the same eight fields.

**Submodule order (when a remote exists):** commit + push HERE first, verify the SHA is on the remote, THEN bump the gitlink in the parent hyMMO repo. Until a remote exists, consumers build against the local jar.

## Release notes

`CHANGELOG.md` is the dev changelog (newest first); `CURSEFORGE.md` is the GitHub/CurseForge front page; `patch-notes/<version>.md` is the per-version user/modder-facing release note (frontmatter `version`/`title`/`type`/`status` + a short summary + bullets + a Related section linking the changelog). Author one per release. **Describe shipped reality, not aspiration.** **Keep these notes mod-agnostic: this is a domain-free library, so do NOT name a specific consumer's internals / unreleased product features (e.g. an MMO's classes or "Pro" menus) - describe primitives in neutral "a consumer" terms. Naming the openly-declared companion exemplar (Kweebec Nightmare) is fine.**

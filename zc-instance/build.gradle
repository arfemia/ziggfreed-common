apply from: "$rootDir/gradle/zc-module.gradle"

// The instance-experience layer: arena, encounter director, match rules, play/queue and results
// screens, presets, leaderboards, plus the lobby and party systems that feed them.

dependencies {
    implementation project(':zc-core')
    implementation project(':zc-presentation')
    implementation project(':zc-loot')
}

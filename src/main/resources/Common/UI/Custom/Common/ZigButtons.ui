$C = "../Common.ui";

// ── ziggfreed-common shared RICH-ROW button style (Button + inner Label) ──────
// Imported per page as:  $Z = "../Common/ZigButtons.ui";  then  Style: $Z.@ZigRichRowBtnStyle;
//
// The generic clickable-list-ROW companion to the ZigRichButton primitive
// (com.ziggfreed.common.ui.ZigRichButton). A `ButtonStyle` (backgrounds + Sounds
// only, NO LabelStyle) for the vanilla `Button` ELEMENT - which, unlike a
// `TextButton`, wraps a child `Label #Label` whose text is pushed via .TextSpans so
// PARAMETERIZED / composite localized text actually substitutes (a TextButton .Text
// prints the literal "{0}{1}"). Authority: the hyMMO repo's
// .claude/research/hytale-rich-text-textspans.md.
//
// THEME-TINTABLE: the native Secondary.png 9-slice patch with a per-state neutral
// Color tint - the SAME retint-ready form as @ZigOptionBtnStyle, so a theme can
// recolour each row via UiRetint.retintButtonStates (overwriting
// .Style.{Default,Hovered,Pressed}.Background.Color) exactly like the dialogue option
// rows. A FLAT hex `Background: #rrggbb` would NOT be tintable (no .Color
// sub-property) - hence the patch. The per-row TEXT style (font / colour / alignment)
// lives on each consumer's inner `#Label`; a selected-row accent is set from Java by
// tinting this instance's .Style.Default.Background.Color (NOT by replacing .Background
// with a bare String, which red-Xes) + the #Label colour. The 9-slice needs a row
// height >= ~24px to render cleanly. For a tab/filter look use the vanilla
// $C.@TertiaryButtonStyle instead. Keep Sounds or the row is silent on click.
@ZigRichRowBtnStyle = ButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #41506a)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #5b6f8c)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #344156)),
  Sounds: $C.@ButtonSounds
);

// PRIMARY-CTA variant of the rich row: a brighter steel tint for a footer / hero
// call-to-action (Switch Class, Accept, View Leaderboard). Same retint-ready
// Secondary.png patch form as @ZigRichRowBtnStyle, so a theme recolours it the same
// way. Pair with the @ZigRichBtn component (or a Button + Label #Label) below.
@ZigRichPrimaryBtnStyle = ButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #7d93aa)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #a8bdd2)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #66798c)),
  Sounds: $C.@ButtonSounds
);

// ── ziggfreed-common reusable RICH BUTTON component (icon + label) ────────────
// Imported per page as:  $Z = "../Common/ZigButtons.ui";  then instantiated:
//   $Z.@ZigRichBtn #MyBtn { @Style = $Z.@ZigRichPrimaryBtnStyle; @LabelStyle = (...); }
//
// A `Button` (clickable container, see ZigRichButton) wrapping an OPTIONAL leading
// icon, the text `Label #Label`, and an OPTIONAL trailing icon. Both icon slots are
// Visible:false until populated from Java via ZigRichButton.iconStart / .iconEnd - so
// the SAME component places an icon at the START or the END (or neither / both) with
// no per-consumer .ui variant. Text is pushed via ZigRichButton.text (.TextSpans).
// Overridable params: @Style (ButtonStyle), @LabelStyle, @Anchor, @Height, @IconSize.
@ZigRichBtn = Button {
  @Style = @ZigRichRowBtnStyle;
  @LabelStyle = (FontSize: 13, TextColor: #e6eef7, RenderBold: false, HorizontalAlignment: Start, VerticalAlignment: Center);
  @Anchor = Anchor();
  @Height = 28;
  @IconSize = 16;
  Style: @Style;
  Anchor: (...@Anchor, Height: @Height);
  LayoutMode: Left;
  Padding: (Horizontal: 8);

  // Leading icon - a UI glyph texture set + shown by ZigRichButton.iconStart.
  Group #IconStart {
    Anchor: (Width: @IconSize, Height: @IconSize, Right: 6);
    Visible: false;
  }

  Label #Label {
    FlexWeight: 1;
    Text: "";
    Anchor: (Height: @Height);
    Style: @LabelStyle;
  }

  // Trailing icon - set + shown by ZigRichButton.iconEnd.
  Group #IconEnd {
    Anchor: (Width: @IconSize, Height: @IconSize, Left: 6);
    Visible: false;
  }
};

// ── ziggfreed-common shared dialogue option button style (de-MMO'd) ───────────
// Imported per page as:  $M = "../Common/ZigButtons.ui";  then  Style: $M.@ZigOptionBtnStyle;
//
// The native Secondary.png 9-slice patch (Border: 12) with a soft steel neutral
// tint; the dialogue page overwrites the per-state .Background.Color from Java
// (DialogueOptionStyle) per the option's decisive action kind. Texture paths are
// "../Common/Buttons/..." - because Common/ and Pages/ are siblings under Custom/,
// they resolve to the game's Common/Buttons whether the engine resolves relative to
// this file or the consuming page. MUST keep Sounds: $C.@ButtonSounds or the button
// is silent on click.

// Dialogue option row button - left-aligned label, soft steel patch, non-bold.
@ZigOptionBtnStyle = TextButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #6f8398), LabelStyle: (FontSize: 14, TextColor: #e6eef7, RenderBold: false, HorizontalAlignment: Start, VerticalAlignment: Center)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #9fb3c8), LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: false, HorizontalAlignment: Start, VerticalAlignment: Center)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #5a6e82), LabelStyle: (FontSize: 14, TextColor: #e6eef7, RenderBold: false, HorizontalAlignment: Start, VerticalAlignment: Center)),
  Sounds: $C.@ButtonSounds
);

// ── ziggfreed-common shared segmented tab / filter button ─────────────────────
// Imported per page as:  $M = "../Common/ZigButtons.ui";  then  Style: $M.@ZigTabBtnStyle;
//
// A center-aligned sibling of @ZigOptionBtnStyle for tab / filter rows (a leaderboard's
// party-size tabs, a paged list's category chips). ONE style covers active + inactive:
// the page overwrites the ACTIVE tab's per-state .Background.Color from Java to a brighter
// tint (the same technique DialoguePage uses to paint an option row by its action kind),
// so there is no separate active style to drift. Keep Sounds or the tab is silent on click.
@ZigTabBtnStyle = TextButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #4a596b), LabelStyle: (FontSize: 13, TextColor: #b6c9de, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #6f8398), LabelStyle: (FontSize: 13, TextColor: #ffffff, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #3a4658), LabelStyle: (FontSize: 13, TextColor: #e6eef7, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Sounds: $C.@ButtonSounds
);

// ── ziggfreed-common shared PRIMARY CTA button ────────────────────────────────
// Imported per page as:  $M = "../Common/ZigButtons.ui";  then  Style: $M.@ZigPrimaryBtnStyle;
//
// A CENTER-aligned, slightly brighter sibling of @ZigOptionBtnStyle for footer call-to-action
// buttons (results-screen View Leaderboard / Play Again). Same Secondary.png patch; label is
// centered (NOT Start - the option style is left-aligned for dialogue rows). Keep Sounds or the
// button is silent on click.
@ZigPrimaryBtnStyle = TextButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #7d93aa), LabelStyle: (FontSize: 14, TextColor: #eef4fb, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #a8bdd2), LabelStyle: (FontSize: 14, TextColor: #ffffff, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #66798c), LabelStyle: (FontSize: 14, TextColor: #eef4fb, RenderBold: false, HorizontalAlignment: Center, VerticalAlignment: Center)),
  Sounds: $C.@ButtonSounds
);

// ── ziggfreed-common shared SORTABLE COLUMN-HEADER button ─────────────────────
// Imported per page as:  $M = "../Common/ZigButtons.ui";  then  Style: $M.@ZigSortHeaderBtnStyle;
//
// A MINIMAL, RIGHT-aligned, small-font clickable header for "click a column to sort" lists (the
// leaderboard Stats-view stat columns + Plays). Muted by default so it reads as a header, not a
// chip; the page paints the ACTIVE (currently-sorted) header's per-state .Background.Color from
// Java via the same style() helper that tints the tabs (default tint mirrors INACTIVE_TINT
// #2f3b49). Right-aligned to line up with the numeric cells below it. Keep Sounds or it is silent.
@ZigSortHeaderBtnStyle = TextButtonStyle(
  Default: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary.png", Border: 12, Color: #2f3b49), LabelStyle: (FontSize: 11, TextColor: #8696a8, RenderBold: false, HorizontalAlignment: End, VerticalAlignment: Center)),
  Hovered: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Hovered.png", Border: 12, Color: #445364), LabelStyle: (FontSize: 11, TextColor: #ffffff, RenderBold: false, HorizontalAlignment: End, VerticalAlignment: Center)),
  Pressed: (Background: PatchStyle(TexturePath: "../Common/Buttons/Secondary_Pressed.png", Border: 12, Color: #2f3b49), LabelStyle: (FontSize: 11, TextColor: #e6eef7, RenderBold: false, HorizontalAlignment: End, VerticalAlignment: Center)),
  Sounds: $C.@ButtonSounds
);

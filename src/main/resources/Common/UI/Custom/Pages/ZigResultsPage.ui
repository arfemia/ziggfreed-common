$C = "../Common.ui";
$F = "../Common/ZigFrames.ui";
$M = "../Common/ZigButtons.ui";

// Generic end-of-game results screen (com.ziggfreed.common.instance.result.ResultsPage):
// outcome header, team-grouped per-player rows (Pages/ZigResultRow.ui), the viewer's
// score-column breakdown, the reward strip (Pages/ZigResultChip.ui), and footer CTAs.
// Shipped once in ziggfreed-common; neutral Zig* ids. Smoke-test in-game.

$C.@PageOverlay {}
$F.@ZigDecoratedFrame {
  @CloseButton = true;
  Anchor: (Width: 660, Height: 752);

  #Content {
    Label #OutcomeTitle {
      Text: "";
      Anchor: (Height: 34, Bottom: 2);
      Style: (FontSize: 24, TextColor: #ffd700, RenderBold: true, HorizontalAlignment: Center);
    }
    Label #Duration {
      Text: "";
      Anchor: (Height: 18, Bottom: 8);
      Style: (FontSize: 12, TextColor: #8696a8, HorizontalAlignment: Center);
    }

    Group { Anchor: (Height: 1, Bottom: 8); Background: #1e2a36; }

    // Team-grouped per-player rows (scrolls past ~4 rows for a large party).
    Group #ResultsList {
      Anchor: (Height: 130, Bottom: 10);
      LayoutMode: TopScrolling;
      ScrollbarStyle: $C.@DefaultScrollbarStyle;
      Padding: (Right: 4);
    }

    // Point breakdown - how the score was earned (5 rows: Base + the score components).
    Label #BreakdownTitle {
      Text: "";
      Anchor: (Height: 20, Bottom: 4);
      Style: (FontSize: 14, TextColor: #ffd97a, RenderBold: true);
    }
    Group #BreakdownList {
      Anchor: (Height: 158, Bottom: 8);
      LayoutMode: TopScrolling;
      ScrollbarStyle: $C.@DefaultScrollbarStyle;
      Padding: (Right: 4);
    }

    // Run stats - raw per-run activity (a second section below the points).
    Label #StatsTitle {
      Text: "";
      Anchor: (Height: 20, Bottom: 4);
      Style: (FontSize: 14, TextColor: #ffd97a, RenderBold: true);
    }
    Group #StatsList {
      Anchor: (Height: 126, Bottom: 8);
      LayoutMode: TopScrolling;
      ScrollbarStyle: $C.@DefaultScrollbarStyle;
      Padding: (Right: 4);
    }

    Label #RewardsTitle {
      Text: "";
      Anchor: (Height: 20, Bottom: 4);
      Style: (FontSize: 14, TextColor: #ffd97a, RenderBold: true);
    }
    Group #RewardList {
      LayoutMode: Left;
      Anchor: (Height: 30, Bottom: 4);
    }
    Label #NoRewards {
      Text: "";
      Visible: false;
      Anchor: (Height: 18, Bottom: 4);
      Style: (FontSize: 12, TextColor: #8696a8);
    }
    Label #PendingNote {
      Text: "";
      Visible: false;
      Anchor: (Height: 18, Bottom: 6);
      Style: (FontSize: 11, TextColor: #e0a030);
    }

    // Claim button (full-width, the primary action) + footer CTAs.
    TextButton #ClaimBtn {
      Text: "";
      Anchor: (Height: 40, Top: 4, Bottom: 8);
      Style: $M.@ZigPrimaryBtnStyle;
      Visible: false;
    }
    Group #FooterRow {
      LayoutMode: Left;
      Anchor: (Height: 36);
      TextButton #ViewLbBtn    { FlexWeight: 1; Anchor: (Height: 36); Style: $M.@ZigPrimaryBtnStyle; Visible: false; }
      TextButton #PlayAgainBtn { FlexWeight: 1; Anchor: (Height: 36, Left: 6); Style: $M.@ZigPrimaryBtnStyle; Visible: false; }
    }
  }
}

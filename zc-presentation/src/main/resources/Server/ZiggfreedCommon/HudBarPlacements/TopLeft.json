{
  "$Comment": [
    "A spot a bar panel can sit at: the top-left, at the same origin as the zone card another mod",
    "draws there, deliberately OVER it: the panel is transparent behind its frame, it attaches after",
    "the card so it draws on top, and the two read fine stacked, whereas sitting below the card and",
    "the mob inspector would push the ledger halfway down the screen. Measured for the tall activity",
    "ledger (the Default panel) and offered to the wide World panel too: one narrow column until the",
    "column is full (thirteen rows), then a second one beside it rather than running off the bottom",
    "of the screen.",
    "",
    "MinHeight is the least height a panel draws at here, whatever its rows add up to. It matches",
    "that zone card, so a ledger of one or two rows still covers the card rather than leaving its",
    "lower half showing beneath a shorter frame; a busier ledger grows past it as usual. Leave it",
    "out on a spot with nothing beneath it.",
    "",
    "Position is a corner preset (TopLeft, TopCenter, TopRight, CenterLeft, Center, CenterRight,",
    "BottomLeft, BottomCenter, BottomRight) plus pixel offsets from the edges that preset pins.",
    "Columns and RowsPerColumn say how the rows spread out here; a panel never opens more columns",
    "than its own document declares, nor fills a column deeper than its document allows. Gap leaves",
    "a band clear across the rows after so many of them, counted from the pinned edge (see",
    "BottomLeft.json, which uses it). Panels lists which panels offer this spot; leave it out to",
    "offer it to every panel. LabelKey names the spot in the HUD settings, in each player's own",
    "language.",
    "",
    "To change a leaf without copying this file, add an entry keyed 'TopLeft' to",
    "mods/ziggfreedcommon/hud-bar-placements.json with only the leaves you want different. To add a",
    "spot of your own, drop another file in this folder: every picker offers it at once."
  ],
  "LabelKey": "ziggfreedcommon.hud.placement.top_left",
  "Position": {
    "Preset": "TopLeft",
    "OffsetX": 16,
    "OffsetY": 90
  },
  "Columns": 2,
  "RowsPerColumn": 13,
  "MinHeight": 124,
  "Panels": ["Default", "Grid"],
  "Order": 10
}

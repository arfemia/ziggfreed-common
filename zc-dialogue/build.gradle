apply from: "$rootDir/gradle/zc-module.gradle"

// The branching NPC dialogue engine and the NPC spawn/placement engine. It sits highest in the
// graph because a placement gate resolves world identity (zc-world), pins chunks through the
// world-eviction seam (zc-cast), and the dialogue page paints through the shared UI primitives.

dependencies {
    api project(':zc-core')
    api project(':zc-presentation')
    api project(':zc-cast')
    api project(':zc-world')
}
